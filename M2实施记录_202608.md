# M2 实施记录（2026-08 审计修复第三批：闭环正确性 P1）

> 依据：`交易审计修复设计文档_V1.md` M2 里程碑。

## 已实施项

### P1-2 降级粒度与判据（training_live_promote_service）
- 判据：裸胜率 wr<0.40 → 期望值判据（n≥30 且 E=wr×avg_profit+(1-wr)×avg_loss<0），
  低胜率高赔率策略不再被误杀；样本不足时只按回撤/总控连亏降级。
- 粒度：删除 `session.trading_mode="paper"` 连坐——单策略降级不再把整个会话切回模拟盘
  （按策略粒度 live_stage=none，其他 live 策略继续跑真金）。

### P1-3 DRL 重训触发接线（rl/system_coordinator）
- check_and_coordinate 恢复 `_should_retrain_drl` 判定并赋值 `action.trigger_drl_retrain`；
  消费端（learning_loop 748 分支）与冷却字段本就存在，接通即生效；仍受 DRL_RETRAIN_AUTO 开关控制。

### P1-6 RL 仓位集成修复（trading_decision_interface）
- 按真实接口调用 `select_action(regime, vol_ratio, dd, losses, streak, use_greedy=True)`，
  从 RLActionResult.position_pct 取建议；异常升级为 warning（不再静默 no-op）。

### P1-7 tier→nature 单一权威
- unified_learning_service 本地映射（long→position）删除，改 import tp_sl_authority.TIER_TO_NATURE；
- trade_nature_resolver 第三份映射（mid→intraday/long→swing）对齐权威。

### P1-9 walk_forward 年化参数化
- validate/_calc_sharpe/_rolling_sharpe 增加 periods_per_year（默认 8760 保持兼容），
  4h/1d/交易级收益传入时年化正确。

### P1-10 因子前视 AST 审计（新模块 factor_engine/lookahead_audit.py）
- AST 提取 shift 第一参数：含一元负号（USub，含 -confirm_bars+1 变量型）或负常数 → blocked；
  变量窗口 shift(lookback) → review（人工复核）；
- 接入两处：midlong_cold_pool（冷池扫描拦截 + review 统计）与 factor_loader（注册表加载拦截，
  review 仅告警）。

### P1-11 learned_weighting 训练卫生
- train()：前 80% fit / 后 20% 校验；校验 IC < 0.02 丢弃新模型（保留旧模型）；
  purge_bars embargo（训练段尾部剔除）生效；min_ic_to_include 特征筛选生效；
  compute_weighted_signal 到期无训练数据 → 一次性告警（防旧模型静默陈旧）。

### P1-12 标签与记忆卫生
- cycle_direction_probability：range_thr（UP/DOWN/RANGE 标签定义）只用训练段前向收益计算
  （原用全样本含测试集 → 标签泄漏）；
- learning_loop outcome batch：regime_at_exit 不再伪造为 entry regime（回退 unknown）；
- memory_decay：_entry_ts 多键回退（ts→discovered_at→ingested_at→extracted_at→created_at→timestamp）；
  写端统一补 ts：trade_memory_miner（盈利/亏损模式）、decision_feedback_service、evolution_scheduler 周提炼。

### P1-4 live 仓位级 7 天补扫（learning_loop_service）
- 新增 `learning_loop_live_outcome_backfill` 任务（10min）：从 AIDecisionLog
  （executed=true、realized_pnl、pnl_updated_at 7 天内）补齐事件级 hook 漏掉的 live 平仓反馈；
  按 decision_log_id 去重（decision_context 拷贝键新增该字段）；persist_trade=True 落 StrategyTrade。

### P1-5 连亏/计数器落库
- SystemCoordinatorState 新增 runtime_state_json 列（迁移 add_coordinator_runtime_state.py 已注册并应用）；
- unified_learning：_restore/_persist_runtime_state + _track_loss_streak/_check_prompt_evolution_trigger 落库，
  重启后连亏保护与提示词进化触发计数恢复。

## 回归测试
- 新增 `backend/tests/test_m2_fixes_regression.py`（8 个纯单元用例）全部通过：AST 前视×3、
  时间戳回退×1、单一权威×1、年化参数×1、learned_weighting 卫生×2（噪声丢弃/信号接受）。
- 全量回归（M0+M1+M2 新用例 + paper_netting/midlong exit/hold_time/fee/executors 既有）：123 个全部通过。
- 迁移 add_coordinator_runtime_state 已应用（system_coordinator_state.runtime_state_json 存在）。

## 备注
- P1-8 分段止盈统一：P0-10 已收口（RISK_USE_LONG_TIER_STAGED_TP 默认 false + v2 门短路），
  long_tier/nature_staged_tp 退役随开关关闭完成，代码级删除留待 M3 清理。
- P1-1 收益率口径已在 P0-3 完成（twm_return_pct）。
