# 运行与盈利路线图（2026-08 审计后定稿）

> 前置：M0/M1/M2/M3 修复全部落地（123+ 测试绿、40 模块导入冒烟绿、隔离 E2E 全链路绿）。
> 本文件回答三件事：现在系统在哪一步、怎么让它端到端跑起来、怎么做才能有实证支撑的正收益。

---

## 1. 生产现状体检（实测结论，2026-08-16）

| 层面 | 状态 | 证据 |
|---|---|---|
| 行情数据管线 | ✅ 健康 | alpha_market 85.6M 根 K 线，5 家所（asterdex/binance/bybit/hyperliquid/okx），最新到当日 16:49；资金费 2.65M 行 |
| 决策循环 | ⚠️ 空转 | alpha_analytics 30 天 11.1 万条决策日志、33.3 万决策快照，但仅 1774 条有盈亏回填 → 几乎全是 hold |
| 交易执行层 | ❌ 未启动 | alpha_arena：accounts=0、full_auto_sessions=0、paper_positions=0、strategy_trades=0、orders=0 —— 从未有账户/会话/成交 |
| 学习闭环 | ❌ 空库 | strategy_memories=0、trade_facts=886、trade_journals=518（仅少量旧痕迹） |
| 因子池 | ❌ 空 | custom_factor_store 无 active 因子；冷池扫描（1130 因子×3 币×多周期）历史报告 0 通过、耗时 4.4 小时 |
| 后端进程 | ✅ 运行中 | PID 3740（uvicorn 8000 端口，/api/system/status 正常） |

结论：基础设施健康，业务链路从未端到端启动过。当前这台机器上的库没有交易历史，
不存在「历史上已亏钱」或「策略失效」的问题——问题是从未完整运行。修复后的代码已就位（磁盘），
待一次受控重启后生效。

## 2. 端到端启动计划（Bring-Up）

### B1. 账户与资金引导（1 天）
1. 在 alpha_arena 创建 1 个 paper 账户（initial_capital 建议 ≥50k USDT 模拟盘，保证仓位粒度）；
   注意仓库存在 SQLite/Postgres 双轨脚本（ensure_account.py 写 SQLite，.env 指 Postgres）——统一用
   .env 的 Postgres，废弃 SQLite 引导脚本（P2-5 遗留项）。
2. 确认 accounts.is_active、auto_trading_enabled、hyperliquid_environment（先 testnet）配置。

### B2. 因子池播种（2-3 天，GPU 加速关键路径）
1. 重跑冷池中长线扫描（midlong_cold_pool.scan_cold_pool_midlong）——已升级 AST 前视审计（变量负移拦截）
   与 fail-closed DSR/PBO 闸门；4h/1d 候选 A/B 级经 validate_and_promote 晋升 active，填充因子池。
2. 旧扫描 4.4 小时/1130 因子是当前最大瓶颈 → 加速（见 §4）：
   - 先上 CPU 多进程（22C/44T 到位后 8-20x）；
   - numba @njit 重写 _robust_rolling_ic / _walk_forward_backtest 内层（5-20x）；
   - torch FP32 向量化跨币批量计算（10-30x，8GB 显存分批；2080Ti 22G 到位后全量）。
3. 目标：扫描压到 <30 分钟，产出 10-30 个有 OOS 证据的 4h/1d 因子。

### B3. 模拟盘会话启动与观察（1 周）
1. 启动 full-auto paper 会话：优先 mid（swing_agent，1h/4h）与 long（trend_agent，4h/1d）层级——
   与实证最强的 TSMOM/横截面动量频段一致；short（scalp 分钟级）先小仓位灰度。
2. 逐日检查闭环四件套：orders → paper_positions → strategy_trades（含 snapshot_id/decision_log_id）→
   strategy_memories（真 Sharpe/回撤口径）→ Gate2 晋升候选。
3. 观察资金费 dry-run（FUNDING_SETTLE_APPLY_PNL=false 只记账）；对拍两周后打开。

### B4. 实盘门槛（2-4 周后，严格 gate）
1. Gate2 硬门槛（已修复口径）：真 Sharpe≥1.0、收益曲线回撤≤10%、单笔≤15%、笔数≥30、与回测偏差≤30%。
2. 先 Hyperliquid testnet → 小仓（≤10%）→ 逐步放大；优先 maker 挂单（0.01% vs 0.035% taker）。

## 3. 正收益路径（实证依据）

调研结论（Liu & Tsyvinski J.Finance 2022、crypto factor zoo、XGBoost 扣成本 walk-forward 等）：

1. 主线：小时/日级 TSMOM + 横截面动量——加密市场最强的可复制 alpha；本系统 mid/long 层
   （1h/4h/1d + 趋势/摆动因子）天然对位。分钟级短线实证上扣成本后多为负，降级为执行优化而非独立 alpha。
2. 资金费率作方向/择时过滤（非纯套利——funding carry 已饱和，实证年化压到近 0）。
3. 成本纪律：单笔 alpha 必须覆盖 ≥10bps（taker 往返）；优先 Hyperliquid maker（0.01%）+ 低频。
4. 验证纪律：任何上线策略必须过 walk-forward + 真实费率/滑点/资金费 + 我们修复的 fail-closed DSR/PBO；
   杜绝 hyperopt 曲线拟合（Freqtrade 社区「回测优秀、实盘崩溃」的教训）。
5. 预期管理：实证界加密动量组合年化 Sharpe ~1.0-1.7（波动极大、回撤 30-50% 正常）；
   目标定在「正期望 + 回撤受控」，不追不菲神话。仓位与杠杆由 Kelly/回撤上限夹紧（已修复的期望值判据）。

## 4. GPU 加速方案（GTX 1070 → 2080Ti 22G）

| 方向 | 改造点 | 预期 | 备注 |
|---|---|---|---|
| ① numba 热点循环 | factor_backtest_scorer._robust_rolling_ic/_walk_forward_backtest、midlong_cold_pool 打分循环 | 5-20x | 现成 numba 0.61；改 ndarray、去 DataFrame |
| ② torch 向量化因子批量 | 冷池扫描跨币×跨 fwd 批量、factor_evaluation_pipeline | 实测 260x（IC 热点） | FP32；8GB 分批；2080Ti 后可 FP16/AMP 再翻倍 |

### GPU 加速落地状态（2026-08-16 已实施）
- `factor_engine/midlong_cold_pool.py` 新增 `_rolling_ic_torch`（单因子）/ `_batch_rolling_ic`（K 因子一次张量调用）
  与 `_rolling_ic_batch`（分发器：GPU 可用走 torch、否则 numpy 逐窗口）；`_robust_rolling_ic` 已改走分发器。
- 实测（GTX 1070，n=1200，window=120）：批量×128 因子 = 258.6ms vs 逐因子 numpy×8 = 4198.7ms，
  偏差 0.00e+00，单因子等效 ~260x；外推全扫描 IC 部分 ≈27s（旧扫描 IC 部分占 4.4h 大头）。
- 下一步：把 scan_cold_pool_midlong 内层按 (tf, sym, fwd) 分组调 `_batch_rolling_ic`（walk-forward 逐因子保留），
  全扫描预计压到 <10 分钟。
| ③ 参数网格并行 | genetic_optimizer/evolution_scheduler 回测网格 → 多进程/joblib | 10-50x（CPU） | 22C/44T 到位后直接受益 |
| ④ ML 因子权重 | learned_weighting | 维持 lightgbm CPU | 数据规模中等，GPU 收益 <2x |
| ⑤ DRL/PPO | rl_position_sizer/trading_env | 暂不上 GPU | env 步进是 CPU 瓶颈；2080Ti（Turing 7.5）后可 AMP + 批量 env |

约束：GTX 1070 无 Tensor Core，禁用 FP16（反而更慢）；2080Ti 22G 到位后：FP16/AMP、更大 batch、
DRL 批量 env、cudf 可用（要求算力 7.0+，Turing 7.5 满足）。


## 6. 运行时根因清单与修复状态（日志深挖代理结论 + 编码损坏根因）

| # | 根因 | 状态 | 处置 |
|---|---|---|---|
| 1 | asterdex 多周期 K 线过期数小时（7734 次警告；WIF/XPL 5-8h 过期）→ 决策层拒开仓 | ⚠️ 运维 | 决策层 fail-closed 正确；需修复采集/回补（尾部小币已退市应移出采集池） |
| 2 | DB 连接泄漏 idle-in-transaction（41,522 警告；最长 137s；6 次强杀） | ✅ 部分修复 | 双命名空间双池是放大器——导入统一后恢复单池；泄漏点仍需 LeakGuard 监控 |
| 3 | Kline 统计查询超时被取消 ×94 | ✅ 随 #2 | 查询取消是泄漏症状；单池后重测 |
| 4 | 因子进化数据深度不足（need=4610/got=500）→ 学习闭环停滞 | ⚠️ 运维 | 代码已有 depth_backfill nudge；需跑回补；GPU 批量扫描（已落地 260x）缓解冷池路径 |
| 5 | AutoCoinSelector 中线无合格币 ×58 | ⚠️ 依赖 #1/#4 | 数据恢复后重评 |
| 6 | 双模块命名空间双重初始化（backend.* vs 根级 database.*，各建一套引擎/池） | ✅ 已修复 | 92 处 legacy 导入统一为 backend.database.*；迁移动态导入同步收敛 |
| 7 | 日志轮转 WinError32 文件占用 ×80 | ⚠️ 运维 | 双日志句柄随 #6 收敛；轮转策略待检 |
| 8 | 启动建索引失败（schema 漂移）×20 | 🟡 低 | 非致命；待对齐迁移 |
| 9 | Hyperliquid 429 限流 ×6 | 🟡 低 | 已有重试；限流预算待调 |

**终极根因**：以上 9 条之上是【团队 PS 5.1 管线损坏全库中文源码（153 文件不可编译）】——
8/13 后所有重启必败，运行中的后端是内存僵尸。已于 2026-08-16 全库修复（见
`全库编码损坏修复记录_20260816.md`），磁盘代码可正常重启。

## 5. 验证与监控闭环

- 每次改动：pytest 全套（新 27 用例 + 既有 96+）+ 隔离 sqlite E2E 冒烟（开仓→风控→平仓→学习落库）。
- 每日：对拍报告（回放 vs 实盘决策一致率；纸面净值 vs 交易所对账单含资金费）。
- 每周：晋升候选审计（Gate2 三口径）、因子衰减报告（factor_decay_eval 心跳）、漂移告警。
- 受控重启窗口：磁盘代码（含全部修复）在下次重启生效；重启前跑 _import_smoke + 迁移幂等检查。
