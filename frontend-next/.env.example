# frontend-next 环境变量示例
# 复制为 .env.local 并按需修改。

# 后端 FastAPI 基址（不含尾部斜杠）
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# WebSocket
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws

# 构建注入的客户端版本（electron:build:prod 会自动带 package.json version）
# NEXT_PUBLIC_VERSION=0.1.0
