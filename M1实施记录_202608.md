# M1 实施记录（2026-08 审计修复第二批：闭环自证）

> 依据：`交易审计修复设计文档_V1.md` M1 里程碑。

## 已实施项

### P0-5 前视修复（4 处）
- `live_pipeline_backtest_engine.py` `_get_funding_rate/_get_fgi`：只取 ≤ts 的最近历史样本（backward），
  未来样本一律不可用（原 min(abs(t-ts)) 最近邻可取 ±1~2 天未来数据）。
- 入场价模型：信号在 bar i 收盘产生，默认按下一根开盘成交（next_open），
  `BACKTEST_LP_FILL_MODEL=close` 仅用于旧口径对比；`entry_bar` 同步修正。
- 资金费相位：从 `i % bars_per_8h`（相对序列起点）改为按 8h UTC 边界（00/08/16）跨边界结算。
- `backtest_engine/backtest_engine.py` `fill_model` 默认 close→next_open（向量化模式本就 shift(1)，无需改）。
- `data_center.py`：`get_klines/_query_best_exchange` 新增 `closed_only` 参数（trade 用途默认 True），
  剔除未收盘 forming bar；缓存键纳入 closed_only；KlineResult 增加标记。

### P0-2 因子衰减监控接线
- `factor_decay_monitor.py`：新增 `_load_status/_save_status`（data/factor_decay_status.json 持久化，重启恢复）；
  `get_factor_weight_penalty` retire 双确认（recent 与 historical 同时低于阈值才归零，否则 0.3 下限）。
- `learning_loop_service.py`：注册 `learning_loop_factor_decay` 任务（默认 6h，env 可覆盖），
  `_tick_factor_decay` 调 evaluate_all_factors 并统计 retired/reduced；接入 trigger_job 映射。

### P0-1 DSR/PBO 多重检验闸门重构
- `dsr_pbo.py` `compute_pbo_simple` 重写为【时序 CSCV】：按时间轴连续切 S 段、C(S,S/2) 组合，
  IS 均值定方向、OOS 衰减过半（含翻号）计过拟合；废除按 ICIR 值 argsort 分组（无时间维度，PBO≈0 恒通过）。
  样本不足/序列过短 → indeterminate（调用方 fail-closed）。
- `compute_dsr_pbo_for_factors` 支持 `ic_series` 参数（PBO 用时间序列，不再用跨币标量）。
- `factor_backtest_scorer.py`：新增 `_rolling_ic_series`（窗口 30 滚动 IC 时序）；score_formula 收集跨币平均
  IC 时序并传入闸门；`_dsr_pbo_gate` 全部 fail-closed：跨币样本 <3、IC 时序缺失、indeterminate、计算异常均拒绝；
  `FACTOR_SCORER_DSR_MIN_SYMBOLS` 默认 3；`n_trials` 动态化 = max(40, active 数+1)。
- `evolution/factor_evolution_loop.py`：删除 `.get(...,True)/.get(...,0.3)` 放行缺省，
  indeterminate/缺字段 → fail-closed。

### P0-4 决策快照精确关联（防归因错配）
- `decision_snapshot_writer.py`：去重签名加入 strategy_id 与 confidence；带 proposal_id/trace_id 的
  快照直接放行不去重（唯一键天然区分决策）。
- `paper_trading_engine.py` 平仓回写：加【方向一致 + 唯一性】约束（48h 窗口内候选不唯一 → 记
  attribution_ambiguous 并跳过，宁缺勿错配）；匹配成功后把 trace_id/proposal_id 存入
  pos._matched_snapshot_id 并写入 TradeOutcome.metadata.snapshot_id（unified_learning 既有
  snapshot_id 拷贝键自动落 decision_context → (snapshot, trade, position) 三方关联）。
- 消费端防御：`local_llm/dataset_builder.py`（SFT 数据集）与 `experience_retriever.py` 只吃
  `executed == True` 的快照。

### P0-9 AI 因子发现候选态
- `ai_factor_discovery_service.py`：注入文件后不再默认 hot_reload（AI_FACTOR_AUTO_ACTIVATE 默认 false），
  候选须经 factor_backtest_scorer.validate_and_promote（OOS 回测 + 新 DSR/PBO 闸门）评 A/B 才晋升；
  结果带 auto_activated 标记 + 运维日志。
- `base_factors.compute_all_factors`：运行期断言——未传 allowlist 时打 warning（每符号一次）；
  `FACTOR_LIVE_ALLOWLIST_ONLY=true` 时自动收敛到 custom_factor_store active 集（fail-closed）。

### P0-3 晋升指标口径修正（真 Sharpe/真回撤/统一收益率）
- `training_live_promote_service.py` 新增 `compute_real_trade_metrics`：real_sharpe（交易级年化，
  年化按平均持仓时长）、equity_dd_pct（累计收益曲线 cumprod(1+pnl_pct) 峰谷回撤）、
  twm_return_pct（已实现盈亏 ÷ 时间加权平均占用保证金）。
- `_run_gate2` 改用真指标；`strategy_validator.py` Gate2：真 Sharpe≥1.0 + 收益曲线回撤≤10% +
  单笔最大亏损≤15%（辅助门槛）；伪 Sharpe 字段退出判定。
- `strategy_learning_service._check_and_promote_strategies`：晋升条件改用 real_sharpe≥0.5。

## 回归测试
- 新增 `backend/tests/test_m1_fixes_regression.py`（9 个纯单元用例）+ M0 用例，共 19 个全部通过：
  回放 backward 取值×2、时序 PBO×3（indeterminate/方向翻转 PBO=1.0/稳定序列通过）、滚动 IC×1、
  衰减双确认×1、快照去重×1、Gate2 真指标×2。
- 既有相关测试 119 个通过；10 个失败为预先存在的环境/接线漂移问题：
  test_e2e_learning_chain 系 PostgreSQL 写入权限（InsufficientPrivilege）；
  test_learning_execution_chain 的 learning_bus 源码断言系旧接线断言（learning_bus 已迁移至
  learning_registry_bridge，全文件 0 处引用且本次 diff 未触碰）——均与本次修改无关。

## 上线说明（运维）
1. `FACTOR_LIVE_ALLOWLIST_ONLY` 默认 false（仅告警）；确认 active 集质量后置 true 强制收敛。
2. 晋升闸门现为 fail-closed：样本不足/时序缺失的因子会被拒（原因写入 reason 字段），
   如晋升管道出现大面积拒绝，先检查 kline 覆盖与滚动 IC 时序长度（≥8）。
3. 回放引擎收益口径变化（next_open + backward 取值 + 8h 相位）会使历史回测收益下修，
   属预期校正；新旧口径对比可用 BACKTEST_LP_FILL_MODEL=close 回旧。
4. AI 因子默认只落文件不激活；手动激活设 AI_FACTOR_AUTO_ACTIVATE=true。
