# M4 实施记录（2026-08-16 晚 · 因子弹药播种 + 运行时冻结修复）

## 背景
M0~M3 修复完成后进入 bring-up 阶段，目标：冷池扫描播种因子 → 晋升激活 → 模拟盘
决策→成交→学习全链路闭环。过程中发现并修复多个 P0/P1 运行时问题。

## 本轮修复清单

### P0-1 系统全进程冻结（日志 WS 监听器死锁）【根因级】
- 现象：19:22:35 起后端全部输出停止（stdout/file 日志、uvicorn access、调度器
  next_run 停在 19:23:01），API 仍可响应，468 线程烧 2339% CPU。
- 定位：py-spy dump 显示 db-pool-monitor 线程卡在 logging.Handler.acquire，
  即某线程持有 logging 全局锁未释放。
- 根因：`SystemLogHandler.emit`（挂在 root logger、WARNING+ 全量）同步调用
  `SystemLogCollector._notify_listeners`，WebSocket 回调在日志路径内同步执行；
  一个死掉的 WS 连接阻塞回调 → 持有 logging 锁 → 所有线程写日志即冻结。
- 修复：`system_logger.py` 监听器通知改为异步队列 + 专用 daemon 线程分发，
  死监听器就地移除，日志路径永不阻塞。
- 附：stdout 重定向管道破裂是第二层冻结源（console handler 阻塞），file handler
  多撑了 ~7 分钟。

### P0-2 custom_factor_store 多进程写入三重加固
- 撕裂读防护：JSON 解析失败不再降级为空目录（旧实现 self._data={} → 随后
  persist 把空目录覆写回磁盘 = 全部因子丢失，曾真实发生）；改为 4 次退避重读，
  读失败保留内存旧数据。
- 空覆写防护：内存为空且磁盘已有非空数据时拒绝 persist。
- WinError 5/32 重试 + 每进程独立 tmp 文件名（多进程共用 ".tmp" 会互相截断）。

### P0-3 冷池扫描批处理两处崩溃
- `_batch_rolling_ic`：closes 未按因子长度截断 → fwd_ret 与因子值长度差 1
  → 广播异常（2396 vs 2395）。修复：尾部截断对齐。
- 同一函数头部截断 bug：阶段1 存的因子值全部尾部对齐，`v[:n]` 取头部会让
  长度不同的因子与收盘序列错位 → 改 `v[-n:]`/`c[-n:]`。
- 阶段2 walk-forward：closes 按各因子长度尾部对齐后再传入。
- 熔断误判：快速异常（病态因子普遍）与真实超时共用计数器，15 个异常即熔断
  中断扫描 → 分开计数（errors 不计入熔断）。

### P0-4 因子验证格式串崩溃
- DSR/PBO fail-closed 返回 pbo=None → `f"{pbo_val:.3f}"` 抛
  unsupported format string，每个候选因子打分中断（后端 validate 任务 40 分钟
  卡在 1/58 空转）。修复：None 时输出 "N/A"。

### P1 其他
- scalp 冻结 K 线防护：asterdex 未上线/低流动性币返回平盘假数据（APT 100根
  high==low、VIRTUAL 120根全平、成交量 0），产生 score=100 垃圾信号；新增
  volsum<=0 或 flat>=90% 即跳过（已拦截 UNI/XPL/VIRTUAL/AMD/APT/AR/NBIS）。
- factor_sync 旧仓库路径：cloud_* 因子 localized_path 指向 /Users/laobao/项目/...
  旧 Mac 路径，启动注册失败刷 ERROR → 文件缺失降级 info 跳过（归档因子须经
  冷池扫描晋升，不自动进实盘注册表）。
- 会话治理：bootstrap 遗留 session fa_3edbeda559（tenant_id=1、从未注册进
  内存调度）修正 tenant 后停止；主会话 fa_10d44c724e 正常运行。
- AUTH_LOCAL_TENANT=326 启用（本机回环免鉴权按 admin 租户）。
- FACTOR_SCORER_SYMBOLS 扩为 9 币种（ASTER/BNB/BTC/ETH/SOL/UNI/VIRTUAL/XPL/XRP）：
  DSR/PBO 需要 >=4 跨币样本，旧配置只有 3 币 → 闸门永远 fail-closed。

## 因子弹药播种结果（活跃池 0 → 5）

| 因子 | 级别 | 周期 | IC | 来源 |
|---|---|---|---|---|
| ai_gen_mrext@1d | A | 1d | -0.070 | 注册表扫描 |
| sma_cross@1d | A | 1d | -0.156 | 注册表扫描 |
| ai_a101_macd_sig_4h | A | 4h | -0.074 | alpha101 公式（DSR/PBO 通过，2133 笔，win 51.2%，OOS sharpe 0.40） |
| ai_gen_vol_rev@4h | B | 4h | +0.031 | 注册表扫描 |
| ai_gen_uncertainty_vola@1d | B | 1d | -0.054 | 冷池扫描通过者（严格复评 B） |

- 注册表引用候选 84/84 全部打分（经典因子 atr/hv/zscore 等 C/D 全拒）。
- alpha101 45 个复评：44 拒（含 IC 0.23 但 sharpe 为负的 bias30_rev_1d——
  闸门正确识别"IC 有、钱没有"）。
- 冷池全量扫描：1115 因子 × 9 币 × 4h/1d，2099 行评分，4 个 A/B 通过者，
  严格闸门复评后 1 个存活（multi-fwd 变体选择通胀其余 3 个的成绩，fail-closed
  闸门守住）。
- 4 个冷池通过者已从 _ai_gen_quarantine 复制到 ai_generated（前视审计全 ok），
  后端热加载确认 26 ai_generated。

## 交易链路状态
- 中线 factor_route 现用全部 5 因子投票（n=5），BNB sell probe 被"趋势破坏
  耐久冷却"正确拦截，BTC buy 被净敞口 190%>150% 正确拦截。
- 持仓：UNI long（+0.49 浮盈，TP+8.1%/SL-4.5%）、ASTER long（-0.17），
  均为 swing/mid 10x，RR≈1.8，exit_state_json 分级止盈机制就绪。
- 等待平仓完成 决策→成交→strategy_trades→学习回填 全链路证明。

## 全链路闭环证明（19:51 完成）
- ASTER swing long（tpl_mid_reversion_9c21ae，10x）19:51:43 被中线离场状态机
  以 trend_broken 平仓（-0.19 分两笔）。
- strategy_trades 2717/2718 落库，decision_context 含 paper_position_id=3125、
  close_reason、exchange 等完整元数据。
- 35 秒后 strategy_memories 更新（total_trades=2, sharpe=-0.5）——
  unified_learning.process_outcome 学习链路生效。
- 决策→成交→平仓→交易记录→学习落库 全链路证明完成。
- UNI 20:05 平仓 +0.92（tpl_mid_range_4a7068）。
- 首批因子驱动中线交易：ASTER -0.19 / UNI +0.92，净 **+0.73**。

## 第二轮修复（20:00-20:30）
- **FactorValue JSON 崩溃**：build_snapshot 把 FactorValue 对象原样注入
  market_summary → 交易循环 last_market_summary JSONB 落库崩溃
  （TypeError: Object of type FactorValue is not JSON serializable）。快照只存
  float(value)。
- **公式因子注册表误报**：active 公式因子 ai_a101_macd_sig_4h 被送进
  factor_service.compute（仅认 registry）→ "not found in registry" 刷 ERROR。
  build_snapshot 现按公式/注册表分路：公式因子走 _factor_history 公式路径。
- **ops 预检缓存**：/api/ops/midlong-factors 每次 18 次全量 K 线查询（GUI 高频
  轮询），加 60s TTL 缓存（7.3s→5.8s，DB 压力大降）。
- 回归测试 27/27 通过。
- 修复后日志观察窗：not found in registry=0、JSON 序列化错=0、交易循环异常=0、
  ERROR=0。

## CPU 100% 根因与 GPU 切换（用户反馈）
- 现象：全部 32 逻辑核 97-100%，GPU 0%。py-spy 抓包：后端线程池内
  torch nn.Linear forward（神经嵌入推理）+ jsonl_store 追加。
- 根因：两套嵌入模型都被强制 CPU 推理——
  ① qaa.knowledge NeuralEmbeddingProvider（bge-small-zh，RAG 检索每 scalp tick 调）；
  ② rag_knowledge_service（bge-large-zh-v1.5，显式 device="cpu"）。
  torch CPU 推理默认吃满全部核心 → 20+ 核 100%。
- 修复：两处改为 CUDA 可用即上 GPU（不可用回退 CPU）；start_server.py 补
  OMP_NUM_THREADS=4 + torch.set_num_threads(4) 兜底（对齐 run_uvicorn_dev）。
- 效果：CPU 100% → 单核 15-65%；GPU 0% → 10%，VRAM 7.8/8GB（bge-large
  device=cuda 4.7s 加载成功）。
- 注意：VRAM 已 96% 占用，2080Ti 22G 到位后余量更大；若 OOM 可让
  qaa bge-small 回 CPU（模型小、耗时可忽略）或开 fp16。

## 用户反馈两大历史 bug 修复（21:00-22:00）
### 「长线开仓就被平」—— 真实 bug，已修复
- 数据：8 笔 swing 持仓在开仓后 1~7 分钟内被 trend_broken 平掉（BNB 2.9min、
  SOL 2.0min、VIRTUAL 6.7min…），每次小亏 + 白烧一次 LLM 复查。
- 根因①（主）：新仓在 _last_llm_run_ts / exit_state_json 无复查记录 → 首个
  manage tick 即触发 LLM 方向复查 → trend_broken 平仓。入场（factor_route 投票）
  与离场（多周期共振复查）用两套信号，入场从不看趋势。
- 修复①：midlong_position_manager 新仓宽限期——首次 LLM 复查必须从开仓时间
  起算 ≥ MIDLONG_POSITION_MGMT_LLM_INTERVAL_SEC(900s)，论点有兑现窗口。
- 修复②：midlong_executor 入场趋势一致性闸——tier bias 与入场方向相反且
  conf≥0.5 直接拒开（与离场守卫读同一 orchestrator 信封），日志
  「趋势一致性闸拦截」。另确认耐久冷却正常拦重复开仓（ASTER/BNB 21:39-21:40）。
- 回归测试 27/27 通过。

### 「短线几乎没有成交」—— 诊断 + 数据实证（非 bug，是闸门在挡亏损）
- 直接原因：精选因子白名单为空——进化链 factor_active_set 0 个 ACTIVE、
  7 个 PAPER id 在引擎 FACTORS 中不存在 → compute_all_factors 空转 →
  分数 0~29 永远低于入场阈值（最后 1 笔短线成交在 16 小时前）。
- 实证：为短线补种 44 个候选（24@1h + 20@15m，alpha101 公式，horizon=scalp）
  全部过 OOS 闸门 → **44/44 拒绝**：IC 普遍存在（最高 |0.24|）但扣费后
  OOS sharpe 全负（-0.85~-2.2）。
- 历史数据同证：短线 388 笔/7 天净 **-7.8**（另 research/pair_research 111 笔
  净 -449，该路径已停）。
- 结论：taker 成本 0.21%/往返下，小时级因子信号无正期望——闸门 fail-closed
  正确拦住了亏损策略。若要做活短线：唯一现实路径是 maker 挂单降本 4~5x 后
  重评，或维持审计结论（中/长线为正收益层：long trend +10.8）。

## 用户反馈「阀门太严/什么都禁止」→ 去阀门专项（22:00-22:40）
### 发现的「400 笔」计数器 bug
- 今日实际开仓仅 **8** 笔（paper_positions）、44 个订单，但
  trader_mental_states.daily_trades=**400**——套利/研究腿（2 秒级开平）被
  全部计入「今日交易笔数」，频率摩擦据此把置信度门槛抬 +50%（亏损日 ×1.5
  = +75%）→ 全天真实信号全部卡死（用户看到的「跑起来就卡死」）。
- 修复：① 频率摩擦降级为「仅提示」（不再硬性拦截）；② record_trade_result
  排除 arb/pair/research/rebate 腿的计数；③ 脏计数器 400 → 8。

### 拆掉的阀门清单
1. 撤掉本 session 加的入场趋势闸（midlong_executor 还原）。
2. 频率摩擦 TRADE_FREQUENCY_TIERS：30/100/300 笔才 +3/6/10%（原 5/10/20/30
   笔 +5~20%），亏损日倍数 1.5→1.2；且不再硬拦（见上）。
3. 耐久冷却：mid SL 12h→2h、long SL 48h→4h、loss floor 4h/12h→30min/1h
   （reentry_cooldown 默认值）。
4. .env：SCALP_USE_VETTED_FACTORS_ONLY=false（放开短线全量因子计算，
   不再因精选池空而 0 分）、PAPER_SCALP_ADAPTIVE_LOSS_CEILING 38→30、
   MIDLONG_AI_MIN_CONF 0.60→0.40。
- 保留的必要防线：冻结 K 线防护、净敞口上限、当日亏损 5% 冻结、
  趋势破坏耐久冷却（已大幅缩短）。
- 追加：组合预算冻结 PB_SCALP_FREEZE_COOLDOWN_SEC 900s→180s（「亏一笔
  冻 15 分钟」→ 3 分钟）。
- 效果（22:01-22:13 实测）：中线重新开仓 2 笔（BNB short + ASTER long，
  均持有中）；scalp XRP 信号 eff=56-57 过闸；频率摩擦硬拦截=0；
  计数器保持真实值 8 不再膨胀。
- 回归测试 27/27 通过。

## 用户最终指令执行（22:50-23:00）：「亏一笔就冻结」删除 + GPU 挖因子
### 冻结机制【删除】，不是缩短
- portfolio_budget._freeze：新增 PB_FREEZE_ENABLED 开关（.env=false），
  false 时**不写任何冻结时间戳**，只启动修复流水线（下架/重挖/替代）。
- .env：REENTRY_SL/LOSS_COOLDOWN_SEC_* 全部 =0（耐久冷却删除）、
  REENTRY_COOLDOWN_SEC=60、REENTRY_MIN_COOLDOWN_AFTER_TP_SEC=60。
- paper 锁强度配置本已处于「关闭」档（不锁仓专注训练），保留账户级
  累计亏损 5% 熔断（按用户口径：看累计金额，不看单笔）。
- 保留：冻结假数据防护、净敞口/集中度拒单（不冻结）、策略级累计亏损
  下架+重挖+替代（adaptation + 因子衰减退役 + 晋升管线）。

### GPU 因子挖掘流水线（_gpu_mine.py，用户核心诉求）
- ~100 个参数化公式（动量/均线交叉/MACD/RSI/布林/通道/波动率网格） ×
  9 币 × {4h,1h}：numpy 向量化求值 → **torch cuda 批量滚动 IC** → 初筛
  → walk-forward 扣成本 → A/B 晋升。
- **83.8 秒**完成全量挖掘，晋升 3 个 4h 因子（1h 仍无过闸——小时级扣费后
  无正期望，与前期实证一致）：
  | 因子 | 级别 | IC | OOS Sharpe |
  |---|---|---|---|
  | gpu_macd3_8 | B | -0.049 | 0.946 |
  | gpu_macd13_34 | A | -0.101 | 0.410 |
  | gpu_rsi14 | A | -0.134 | 0.583 |
- 活跃因子池 5→7；ai_a101_macd_sig_4h 被复检退役（衰减→下架，替代因子
  已就位——正是「下架重挖找替代」的循环）。
- GPU 挖掘独立进程运行，不占用后端 CPU（显卡批量 IC，CPU 仅向量化求值）。

## 短线专项攻坚（23:00-23:20）—— 短线终于开仓
### 排查链（短线信号全链走查）
- 分数已恢复（ASTER 58 / XPL 95 / XRP 56-57），Gate 通过后信号在 EV 闸后
  **静默消失**：无拦截日志、无订单、无异常。
- 定位：portfolio_budget.evaluate_open **实测挂起 >2 分钟**（日VaR历史模拟
  + 大查询），是热路径静默卡点——信号死在里面，整轮被 190s hang 看门狗
  强制重开，后段币种永远执行不到。同时 FlashVeto LLM（每单 20-50s）与
  scalp 成熟度 warmup +20 收紧也在拦弱信号。
- 修复：① paper 模式跳过组合预算慢检查（PB_PAPER_SKIP=true，账户级累计熔断
  仍由当日亏损 5% 兜底）；② SCALP_VETO_MODE=off；③ 成熟度负向收紧对
  paper 全部 nature 豁免。
- **效果：19 小时来首批短线仓位开仓**——XPL long 23:09（1283 张，10x，
  TP+1.14%/SL-1.26%）、XRP long 23:16（99 张，TP+2.24%/SL-1.35%），
  均在持有中，风控结构完整。

## 热路径去 LLM（用户确认执行，23:30）
- 中线持仓方向复查 + 滚仓 + hold-timeout 复查全部改为确定性规则版
  （多周期共振投票：orchestrator bias + indicators 4h/1d macd/trend 多数决；
  仅 4h 与 1d 同时反向才判趋势破坏）。MIDLONG_REVIEW_LLM=true 可回滚 LLM 版。
- 交易链现为 100% 确定性 Python：因子 → IC 加权信号 → 规则开平仓 → 盈亏落库。
- 回归测试 27/27 通过。
- 效果：当前 8 仓在手（6 scalp + 2 swing）——短线已进入持续交易、数据积累状态。

## 回测引擎三处纠错（reviewer 反馈核实 + 修复）
1. **1w 周期未登记**：_PERIOD_HOURS/_PERIOD_FWD_BARS 缺 1w → funding/年化
   按 1 小时算（168 倍偏差）。已补 1w=168h/fwd=1（1M 与 1m 大小写碰撞，不登记）。
2. **回测数据源换 binance 8 年**：_load_klines 原走 data_center purpose=trade
   （强制 active_exchange=asterdex，3 年）。改为 purpose=research + closed_only，
   默认 FACTOR_BACKTEST_KLINE_EXCHANGE=binance；1d lookback 1000→3000。
   实测 BTC/ETH/BNB/XRP 1d ≈3000 根、1w 468 根周线（BTC 自 2017）。
3. **GPU 挖矿绕过防过拟合闸门**：_gpu_mine.py 原用 _grade_from_metrics 直接
   status=active。改走 validate_and_promote → score_formula → DSR/PBO 多重检验
   （fail-closed），GPU 的 IC/walk-forward 只作初筛。
- 回归测试 27/27 通过。

## 环境/进程
- 后端 PID 25568（回测数据源修复后重启），health 200。
- 冷池扫描 ~30 min 完成（旧口径 4.4h），GPU 批量 IC 生效。
- 额外修复：market_data_v2_scheduler 同步 DB 查询移出事件循环线程
  （py-spy 实锤 MainThread 曾卡在 psycopg _execute_send → 全 API 冻结风险）。
