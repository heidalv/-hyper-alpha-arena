# 长线趋势策略重构设计 V2（2026-08-16）

> 目标读者：开发/策略负责人。本文档基于对现有长线链路（trend_agent → master_execution
> → midlong_executor → midlong_position_manager → exit 状态机）的逐环节调研，输出可落地的
> 重构设计。原则：**方向靠长线、时机靠中短线、拿住靠规则、滚仓靠纪律、退出只认结构**。

---

## 1. 调研总结：现状链路与问题清单

### 1.1 现状链路（实测代码走查）

| 环节 | 现状实现 | 问题 |
|---|---|---|
| 趋势判定 | trend_agent.analyze_direction：LLM + build_trend_evidence（全量 market_envs 证据块 + 深度上下文 + onchain 请求）| ① 输入是未过滤的庞杂数据（几十项指标全量注入），趋势判定混入日线以下噪声；② LLM 不可复现，同一天两次分析可能不同；③ 分析频率过高（每 tick 可触发） |
| 开仓 | execute_midlong_open(tier=long) → authority/regime/组合预算闸 | ④ 开仓只看长线方向，不看中短线时机——趋势向上但 4h 正在回调中段就进场，进场即浮亏、被复查误杀 |
| 持仓管理 | 每 45s tick：规则维度（分档止盈/反转/无进展/回撤）+ 15min 复查（已改规则版）+ hold-timeout 复查 | ⑤ 分析过于频繁：1d/1w 周期的仓位 45s 就被复查一次，95% 复查无信息量还烧资源；⑥ 分档止盈/保本线与 mid 同机制（只是参数不同）——50% 进度就推保本会过早离场 |
| 退出 | trend_broken（复查）/ no_progress(72h) / emergency_drawdown / bias 反转(conf≥0.68 且≥72h) / 分档 TP / trailing | ⑦ 退出信号混杂：日线噪声也能触发；⑧ 缺少「结构破坏」这一唯一权威退出信号；⑨ no_progress 72h 对趋势单过短（一轮趋势通常数周） |
| 数据/记忆 | RAG 文本检索（4 个 collection 混取 12 条）+ LLM 深度上下文 | ⑩ 检索结果与当前市场周期无关、不可结构化；⑪ 未按「趋势周期」沉淀每轮趋势的 R 分布/胜率，无法为下一次趋势提供先验 |

### 1.2 实测证据（本会话收集）

- 近 7 天唯一正收益层 = long trend_follow（+10.82），但只有 5 笔、持有 10h~9.6d 不等——样本小、离场仓促（breakeven_tp/trend_review_close 过早收割）。
- 开仓后 1~7 分钟被 trend_broken 平掉的 8 笔里包含 BTC/ETH/SOL——入场未做中短线时机确认 + 复查过频的直接后果。
- 回测数据源（已切换 binance）：BTC/ETH/BNB/XRP 1d 各 ~3000 根（≈8.2 年，BTC 自 2017）；SOL ~2196 根；1w 聚合 BTC 468 根周线。ASTER/VIRTUAL 等新币 binance 日线偏薄（~330~615 根），不参与 L1 周线判定。

### 1.3 问题归因（一句话版）

用「分钟级频率 + 未过滤数据 + 与中短线相同的处置机制」去跑「周线级趋势」——频率错配、数据错配、机制错配，三者叠加导致进场被误杀、持有被过早收割、趋势利润拿不满。

---

## 2. 设计目标与原则

1. **少而精**：长线仓位数 ≤ 4（核心币 5 选 3~4），只做 L1 趋势明确的行情。
2. **进场准**：方向（L1）与时机（L2）分离，时机不达标不开仓。
3. **拿得住**：开仓后只认结构破坏退出；日线以下任何噪声不触发平仓。
4. **滚起来**：金字塔加仓是长线仓的核心盈利来源（趋势单 60%+ 利润来自加仓段）。
5. **可复现**：交易路径 100% 确定性规则；LLM 只做非交易路径的深度复盘。
6. **防过拟合**：每个 L1/L2 特征必须过现有 OOS 闸门（walk-forward + DSR/PBO），且只在趋势 regime 内采样评估。

---

## 3. 总体架构：三层信号

    L1 趋势层（方向，规则化，1d/1w，日频评估）
       trend_state = {up / down / sideways} + strength(0-100) + 结构目标位
    L2 时机层（择时，中短线因子归一，4h/1h，每根K线评估）
       timing_score = Σ wᵢ × zᵢ(归一因子) → enter / add / wait
    L3 执行层（仓位/风控/退出，实时）
       波动率目标仓位 + 分批次金字塔 + 周线 Chandelier 止损 + 结构破坏唯一退出

三层职责分离：L1 管「做什么方向」，L2 管「什么时候动手」，L3 管「下多重、怎么拿、怎么走」。

---

## 4. 详细设计

### 4.1 趋势层 L1：规则化趋势判定器（新增模块 trend_layer.py）

**输入**：1d K 线（核心币聚合 1w 视图；薄数据币用 1d 视图降级）。

**规则投票（确定性，多数决）**，每个信号 ±1 分：

| 信号 | 计算 | 权重 |
|---|---|---|
| EMA 排列 | EMA20 > EMA50 > EMA100（1d）| +1 |
| 结构 | 价格 > 最近 60 日高点 × 0.98（上升结构）| +1 |
| 动能 | ADX(1d,14) ≥ 20 且 +DI > -DI | +1 |
| 长周期 | 价格 > EMA200（1d）| +1 |
| 回撤不破 | 最近一次回调低点 > EMA100 | +1 |

- score ∈ [-5, +5]；score ≥ +3 → up；≤ -3 → down；其余 sideways。
- strength = score/5 × 100。

**Phase A 回测结论（binance 8 年，20 日前向收益，已实跑）**：
- up 状态：n=3519，20 日 +8.54%，命中 54.1%，t=14.08 —— **强烈有效**（vs sideways +2.65%）。
- down 状态：n=3473，20 日 +2.51% —— **加密货币长偏下「down」不能直接做空**（做空长期仍涨）。
- 设计修正：**L1 为多头单边趋势**——up → 允许做多；down → 仅禁止做多/平多（不主动做空）；
  做空需额外叠加「资金费率极端 + 结构破坏 + 短周期确认」三条件（Phase B 评估），否则默认 flat。
- 结构目标位：最近 swing high/low + 1×ATR(1w) 投影（供加仓/追踪参考）。
- **评估频率：每日一次**（1d 收盘后），事件驱动（新 1d K 线形成才重算）。

**验证**：用现有 walk-forward 引擎对该投票器做 3 年回测（方向命中率、趋势段平均 R），目标：up/down 判定下 20 日收益均值 > 0 且显著性过 t 检验。

### 4.2 时机层 L2：进场择时（Phase B 实跑后简化）

**Phase B 实证结论（binance 8 年，244 次 L1 翻多事件）**：
- 立即进场：20 日 +8.12%，t=3.44；
- 等首次回调进场：+2.22%，t=1.31 —— **等回调把优势打没了**。

**原则（数据驱动）**：加密货币日线趋势是**动量延续型**，不是均值回归型。
- **入场**：L1 翻多 → **立即进场 50%**（不等回调）。
- **加仓/滚仓**：L1=up 且价格**创 60 日新高**（动量延续确认）→ 加仓；不是回调加仓。
- **pullback_z 特征仅作过滤**：避开单日暴涨尖峰（z > 3σ 的极端 bar 不追），
  不做回调进场信号。
- **禁止条件**：L1=sideways；单日波动率极端（ATR 周变化 > 3σ）。

### 4.3 执行层 L3：仓位、金字塔、止损止盈、退出

#### 4.3.1 仓位（波动率目标）

- 目标风险预算：单笔保证金 ≤ 权益 × 5%（趋势单单笔风险小、拿得久）。
- 名义 = 风险预算 × (2×ATR(1w))⁻¹ × 权益 —— 波动越大仓越小（复用 ATR 缩放逻辑）。
- 首次开仓 = 目标仓位的 **50%**（试探仓）；L2 确认后 24h 内补足到 100%（不追高）。

#### 4.3.2 金字塔（重点补仓/滚仓）

- 仓位序列：1R → 2R → 3R 处各加 0.5、0.35、0.25（相对首次名义），总名义 ≤ 2.1× 首仓。
- 每批加仓条件（Phase B 修正为动量延续）：L1=up 未变 + **价格创 60 日新高** +
  浮盈达到对应 R 位（硬门槛，防摊平）。
- **分批次止损**：每批的 SL 设在该批成本价 -1×ATR(1w)（批次止损），整体最差情况 = 首仓亏 2R + 后批零盈亏——**趋势单金字塔的最坏回撤有界，这是可行的核心**。
- 加仓执行链复用现有 5 层门控（_exec_pyramid），只替换准入条件为「L1+L2+R 位」。

#### 4.3.3 止损（长线独有：周线 Chandelier）

- 初始止损：入场价 ∓ **2×ATR(1w)**（结构下方，给足波动空间）。
- 追踪：持仓期间以「最高收盘价 - 2×ATR(1w)」为移动止损（Chandelier 周线版），只在 1d 收盘时更新（日频，不追盘中噪声）。
- **废除**：长线仓的「50% 进度推保本」（现有 breakeven 机制对长线改为 2R 后才允许抬到成本，且由 Chandelier 自然完成，不单独设保本档）。

#### 4.3.4 止盈（长线独有：让利润奔跑）

- **不设固定 TP**。趋势单止盈 = 结构目标位（L1 提供的 swing 投影）到位的分批减仓：
  - 到达结构目标 100% → 减 50%；
  - 剩余仓位交给 Chandelier 追踪（趋势延续就拿满）。
- **废除**：长线仓使用与 mid 相同的分档 TP/锁利机制（TIER long 现有 lock_stages 只保留为兜底，触发阈值提到 ≥3R）。

#### 4.3.5 退出（唯一权威：结构破坏）

- **结构破坏**（唯一主动退出信号）：L1 翻转（score 从 +3 以上跌到 ≤0 或负）或 周线 Chandelier 被打穿 → 全平。
- 所有日线以下信号（4h 反转、单周期 bias、15min 背离）**一律不触发平仓**，只影响加仓节奏。
- 保留的兜底保护（不误伤正常趋势）：
  - 极端回撤：持仓保证金回撤 ≥ 60% → 减半仓（非全平）；≥ 80% → 全平（紧急保护）。
  - no_progress：仅当 hold ≥ **30 天** 且峰值从未到 +1R 才离场（现有 72h 对长线废除）。
  - 分析复核（规则版，日频）：4h 与 1d 同时反向且 L1 已转 sideways → 启动结构破坏评估。

### 4.4 分析频率与 LLM 定位（用户明确要求）

| 路径 | 频率 | 实现 |
|---|---|---|
| L1 趋势判定 | 1d 收盘后 1 次 | 纯规则 |
| L2 时机 | 每 4h K 线 1 次 | 纯规则（z-score 因子）|
| L3 管理 | 每 tick 实时，但阈值全部基于 1w ATR | 纯规则 |
| 长线仓复盘 | **每日 1 次**（替代 45s/15min 轮询）| 规则报告 + **LLM 深度复盘（非交易路径）** |
| 周报 | 每周 1 次 | LLM：趋势质量、R 分布、下轮计划（写结构化记忆）|

- 长线仓的管理 tick 节流：MIDLONG_POSITION_MGMT_LLM_INTERVAL_SEC 只对 LLM 维度生效；新增 LONG_REVIEW_CADENCE_HOURS=24——长线仓的 L1/结构检查每日一次，规则止损（Chandelier 日频更新）实时生效但不在 45s 轮询中做长线专属决策。

### 4.5 数据治理与记忆（防过拟合、让记忆有用）

1. **特征白名单**：L1 用 5 个结构信号、L2 用 5 个时机特征——全部必须过现有 OOS 打分闸门（walk-forward + DSR/PBO），拒绝「全量指标喂 LLM」。
2. **采样过滤**：只在趋势 regime（L1=up/down 期间）采样做 IC/回测；sideways 样本一律不进训练——这是防过拟合的关键一步（现状把全周期数据混在一起评估）。
3. **记忆改造**：废除长线路径的 RAG 文本检索，改为**结构化趋势记忆表**：
   - 每轮趋势归档：起止时间、L1 score 曲线、进场时机分、加仓点位、每批 R、退出原因；
   - 表结构：trend_cycles(symbol, start_ts, end_ts, l1_score, entry_timing, batches[], exit_reason, total_r)；
   - 用途：下一轮趋势开始时读取同类历史（相同币种×相同 L1 结构）的 R 分布做仓位校准与退出耐心参考；LLM 周报读取该表生成定性总结（非交易路径）。
4. **验证纪律**：任何新增特征/参数改动必须附带 OOS 对比报告（改前 vs 改后），未过闸不得上线（沿用现有 fail-closed 晋升）。

---

## 5. 参数草案（初始值，待回测校准）

| 参数 | 初始值 | 说明 |
|---|---|---|
| L1 阈值 | ≥+3 up / ≤-3 down | 5 信号多数决 |
| L1 评估频率 | 1d 收盘后 | 事件驱动 |
| L2 入场 | L1 翻多立即进场 50% | 不等回调（Phase B 实证）|
| L2 加仓 | 创 60 日新高 | 动量延续确认 |
| L2 禁止 | L1=sideways 或单日波动率 >3σ | 极端不追 |
| 首仓 | 目标仓 50% | 试探 |
| 加仓 R 位 | 1R/2R/3R | 0.5/0.35/0.25 序列 |
| 初始止损 | 2×ATR(1w) | 结构下方 |
| 追踪止损 | 最高收盘 - 2×ATR(1w) | Chandelier 日频 |
| 金字塔 | 每 1R 加 0.5/0.35/0.25（封顶 3 次）| Phase C/E 实测：总R+36%、回撤不放大 |
| 结构目标减仓 | 到达目标减 50% | 其余追踪 |
| no_progress | 30 天且峰值 < 1R | 兜底 |
| 极端回撤 | 60% 减半 / 80% 全平 | 紧急保护 |
| 长线复盘频率 | 24h | 规则+LLM 日报 |

全部参数在 Phase D 用 binance 8 年 1d 数据（已切换数据源）+ 现有 walk-forward 引擎做敏感性网格（±30%），输出参数-收益曲面，取平台区（非极值点）——防过拟合。

---

## 6. 可行性评估

### 6.1 可复用（现状已具备）

- 因子归一器（symbol+周期隔离 z-score）、IC 加权投票（factor_route 同款）；
- walk-forward + DSR/PBO OOS 闸门（fail-closed 晋升）；
- GPU 挖因子流水线（_gpu_mine.py，可扩网格挖 L1/L2 特征）；
- tier 保护参数（TIER_LONG_MIN_HOLD 72h 保留为底线）；
- 规则版方向复查（已落地，可作为 L1 结构破坏的初版）；
- 金字塔 5 层门控执行链（_exec_pyramid）；
- 分批次止损/持仓结构（paper_positions 已支持分批）。

### 6.2 需要新增/改动

| 项 | 类型 | 工作量 | 说明 |
|---|---|---|---|
| trend_layer.py（L1 判定器）| 新增 | M | 纯规则，~150 行 + 回测验证 |
| entry_timing.py（L2 归一融合）| 新增 | M | 复用归一器/IC 加权 |
| long 专用管理（频率/Chandelier/结构退出）| 改造 | L | 替换 midlong_position_manager long 分支 |
| 金字塔准入条件 | 改造 | S | 替换 _exec_pyramid 准入 |
| no_progress/分档 TP/保本（长线档）| 改造 | S | 阈值与语义替换 |
| 结构化趋势记忆 | 新增 | M | 新表 + 归档/读取 |
| 长线仓复盘节流 | 改造 | S | LONG_REVIEW_CADENCE_HOURS |

### 6.3 风险与对策

1. **数据深度**：ASTER/VIRTUAL 等 1d 仅 223~334 根 → 1w 视图不可用；对策：薄数据币禁用 L1 周线判定（降级 1d 结构），或从核心币池开始（BTC/ETH/SOL/BNB/XRP）。
2. **趋势单回撤期心理**：2×ATR(1w) 止损在极端行情可能 -8%~-12%（名义 5% 风险预算下单笔亏权益 0.5%~0.6%）——可接受，且分批次止损保证最坏回撤有界。
3. **加仓即套**：L2 时机 + R 位双门槛防摊平；若 1R 处加仓后回落，该批 SL 在成本 -1×ATR(1w)，单批亏损 ≤ 0.35×2×ATR 名义——有界。
4. **过拟合**：参数取平台区 + 特征过闸 + 只在趋势 regime 采样，三重防线。
5. **现有路径并行**：Phase 期间保留旧路径（env 开关），新路径 shadow 对比。

---

## 7. 实施计划（分阶段，每阶段独立验收）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| A | trend_layer L1 判定器 + 3 年回测 | 方向命中率显著 > 50%（t 检验），趋势段平均 R > 0 |
| B | entry_timing L2 + 特征 OOS 验证 | 5 特征全过闸；入场后 24h 平均不亏 |
| C | long 专用管理（频率/Chandelier/结构退出/金字塔准入）| 模拟盘对比：持有期 ≥ 5 天中位数，趋势利润占比提升 |
| D | 结构化趋势记忆 + 周报 LLM | 新趋势开仓能读到历史同类 R 分布并校准仓位 |
| E | 全链路回测 + 参数网格 | 3 年 OOS 净收益 > 0，Sharpe ≥ 0.8，最大回撤 < 25% |

每阶段产出实施记录（沿用 M0-M4 模式）并跑回归测试。

---

## 8. 附录：与现有代码的映射

| 设计项 | 现有代码 | 改动点 |
|---|---|---|
| L1 | trend_agent.analyze_direction（LLM）| 新增 trend_layer.py，LLM 保留给周报 |
| L2 | factor_route 投票（mid 用）| 新增 entry_timing.py，因子库扩充 |
| 止损 | midlong_position_manager SL/exit_state_json | long 分支改 Chandelier |
| 止盈 | exit/tier_exit_strategies（分档 TP/保本/trailing）| long 档改结构目标 + 追踪 |
| 退出 | midlong_exit_guard + no_progress + trend_broken | 统一为 L1 结构破坏 + 兜底 |
| 金字塔 | _exec_pyramid + 5 层门控 | 准入条件替换 |
| 记忆 | RAG 检索 | 新增 trend_cycles 表 + 归档逻辑 |
