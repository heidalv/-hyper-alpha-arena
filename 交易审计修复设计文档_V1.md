# 短期/中期交易逻辑审计 — 根因分析与修复设计文档 V1

> 前置文档：审计报告（_audit_short_mid_202608.md，含全部行号证据）。
> 本文档把每个缺陷落到【根因 → 影响面 → 修改设计 → 迁移 → 测试 → 验收 → 回滚】。
> 状态：V1 定稿（P0-1~P0-10 全部经根因追踪核实，含精确修复行号）。

---

## 1. 根因总纲（六类系统性根因）

审计发现的 40+ 个缺陷不是孤立笔误，而是六类模式性根因的产物。修复设计按模式推进，避免打地鼠。

### R1 重复实现与权威分叉（同一事实多处定义，各改各的）
- 分段止盈三套并存：_run_unified_staged_tp（paper_trading_engine，落库 tp_level_reached）、
  long_tier_staged_tp（full_auto，纯内存）、nature_staged_tp（exit_state_json 持久化）——状态口径互不一致。
- 两套因子引擎：backend/factor_engine/（死代码，0 导入）与 backend/services/factor_engine/（运行时）。
- TIER_TO_NATURE 三份：tp_sl_authority(long→trend_follow) / unified_learning_service:27(long→position) /
  trade_nature_resolver.py:24-28(mid→intraday,long→swing)。
- 两套回测引擎：backtest_engine/（fill_model 默认 close）与 live_pipeline_backtest_engine（资金费/FGI 最近邻前视）。

### R2 闭环“接线缺失”——子系统建好但从未接入运行路径（死代码）
- DSR/PBO 闸门：输入无时间维度，PBO≈0 恒通过（且 3 币场景 fail-open 跳过）。
- factor_decay_monitor：evaluate_* 无任何调用点，penalty 恒 1.0（startup.py:358 只是 import，不注册任务）。
- DRL 重训：trigger_drl_retrain 全库无赋值点；_should_retrain_drl 与回填链路悬空。
- walk_forward_validator.validate()：生产零调用（仅 dashboard get_status）。
- RL 仓位 sizer：trading_decision_interface.py:166-174 调用不存在的接口，异常被吞，从未生效。
- DDG-DA：concept_drift_detector.py:254 NameError 被吞。
- entry_confidence_gate:61-64 tier_key=='scalp' 永假分支。
- 共性根因：模块完成时只做了冒烟验证，没有【接线验收】——grep 调用点数量、跑一次真实调度。

### R3 指标口径错配（同名不同义，跨模块直接比较）
- StrategyMemory.sharpe_ratio = 盈亏符号 EMA ∈[-1,1]，被 Gate2 拿去和 1.0 比（strategy_learning_service:187 用 0.5）——
  同一字段两处消费阈值还不同。
- 晋升收益率 = 已实现盈亏 ÷ 累计换手名义额（turnover），与回测 ROE% 直接比偏差。
- max_drawdown 存的是“单笔最大亏损”，被 Gate2 当“资金曲线回撤”×100 比对。
- walk_forward _calc_sharpe 年化硬编码 sqrt(8760)（h1 假设），频率无关。

### R4 点-in-time（前视）违反
- 回放引擎 _get_funding_rate/_get_fgi：min(abs(t-ts)) 最近邻可取未来样本（±1~2 天）。
- backtest_engine fill_model 默认 close：看到收盘价后按收盘价成交。
- forming K 线入库并被当已收盘 bar 消费（新鲜度门 ≤2 周期+60s 永远放行）。
- 冷池前视防护正则只拦字面 .shift(-数字)，变量负移绕过。
- cycle_direction_probability 标签阈值用全样本（含测试）中位数。
- learned_weighting 全量历史 fit 后即用于当下预测（无 embargo/purge）。
- dataset_builder 事件时间戳按服务器本地时区解释（部署时区耦合）。

### R5 PnL/资金记账错误
- 三处双计：_paper_position_pnl(original_size+partial)、耐久冷却(unrealized+partial)、
  以及分批止盈 total_pnl 已含 partial 的重复口径。
- TP3 空单追踪止损漏乘 side_dir → SL 放到现价下方，触发后以低于市价成交，虚增浮盈。
- profit_drawdown_guard 锁利 SL 非单调（回撤后下调）。
- 紧急 SL 固定 5%，20x 下越过爆仓价；update_position_tp_sl 不经过 _ensure_sl_inside_liq。
- 默认 demo 档不结算资金费，而入场闸门 funding_net_rr_ok 按费率拦单——成本假设永不兑现。

### R6 状态非持久 / 进程内态
- reentry_cooldown 连续亏损历史、unified_learning 连亏 streak/计数器、concept_drift 序列、
  long_tier_staged_tp 档位、DRL 冷却——全部内存态，重启清零。

---

## 2. 逐项修复设计

### P0 组（资金安全 + 闭环自证，先修）

#### P0-1 多重检验闸门（DSR/PBO）重构
- 问题：dsr_pbo.py:169-171 按 ICIR 值 argsort 分组模拟 CSCV，输入无时间维度 → PBO≈0 恒通过；
  factor_backtest_scorer DSR_MIN_SYMBOLS=4（实配 3 币）fail-open；n_trials 固定 40。
- 根因：R2（CSCV 被简化成“值排序”）+ 默认参数与部署现实脱节。
- 影响面：【待并入代理结果：score_formula 的晋升判定分支与调用方清单】
- 设计（草案）：
  1) 数据层：为每个候选因子在评估窗口内产出【因子×时间 ICIR 矩阵】（滚动窗 IC 或按 bar 分片），
     存 factor_evaluation_pipeline 的评估上下文；
  2) compute_pbo_simple 改为接受矩阵，严格按时间轴切 S 片、组合 C(S,S/2) 做 CSCV，
     PBO = P(IS 最优因子在 OOS 中排名差于中位)；S 取 8~16，组合>50 时固定种子采样；
  3) 输入仍为标量列表的调用方降级为 indeterminate 且【fail-closed】（当前为 fail-open）；
  4) DSR_MIN_SYMBOLS 默认改 3，符号数不足时 fail-closed 并显式记录 skipped_reason；
  5) n_trials 改为 max(真实候选数, 40)，由调用方传入。
- 迁移：无 schema 变更；报告 JSON 增加 pbo_method/trial_matrix 字段。
- 测试：构造“纯噪声因子×1000”用例断言通过率≈0；构造“真实动量因子”断言不被误杀；旧函数兼容断言。
- 验收：DSR 报告字段可追溯；3 币场景不再静默跳过。
- 回滚：保留旧函数 compute_pbo_simple_legacy，开关 FACTOR_SCORER_PBO_V2 可切回（默认开 V2）。

#### P0-2 因子衰减监控接线
- 问题：factor_decay_monitor.evaluate_all_factors 无调用点 → penalty 恒 1.0；record_ic 只写内存。
- 根因：R2（startup 只 import 不注册任务）。
- 影响面：失效/反向因子永远满权重参与合成（factor_evaluation_pipeline:220、midlong/scalp active set 的 IC 记录全部空转）。
- 设计：
  1) 在 learning_loop_service 注册 factor_decay_eval 任务，默认每 6h 跑 evaluate_all_factors
     （评估窗 FACTOR_DECAY_EVAL_BARS，默认最近 500 根）；
  2) _ic_history 落库：新表 factor_ic_history(factor_id, symbol, timeframe, ic, bar_ts, created_at)，重启可恢复；
  3) get_factor_weight_penalty：status 缺失时保持返回 1.0（冷启动安全），任务运行后 penalty 生效；
     penalty 下限钳制（≥0.3），防一次误评把因子权重归零；
  4) 衰减达阈值（|IC| 连续两窗低于 min_ic 或符号反转）→ 写 lifecycle 降级事件，交 active_set_policy 降级/退冷池（不直接删因子）。
- 迁移：新增任务 id；_decay_status 内存缓存 + DB 真相。
- 测试：注入“IC 归零”序列断言 penalty 下降并在阈值触发降级事件。
- 验收：调度日志出现 factor_decay_eval 心跳；面板可见 IC 历史曲线。

#### P0-3 晋升指标口径修正（真 Sharpe + 真回撤 + 统一收益率）
- 问题：Gate2 用伪 Sharpe(sign-EMA) 与 1.0 比；max_drawdown 是单笔最大亏损；收益率是 pnl/turnover。
- 根因：R3。
- 影响面：training_live_promote_service._run_gate2 是模拟→真金晋升的唯一门槛；
  strategy_learning_service:187 用伪 Sharpe 0.5 做“晋升模板”。
- 设计：
  1) StrategyMemory 新增 real_sharpe、equity_max_drawdown 两列，由 _run_gate2 从 strategy_trades 现算（不新增写路径）：
     real_sharpe = mean(pnl_pct)/std(pnl_pct) × sqrt(年化系数)，年化按平均持仓时长换算；
     equity_dd = 1 - min(累计净值)/峰(累计净值)，净值由 closed 交易 pnl 按 closed_at 排序累加；
  2) Gate2 改用 real_sharpe ≥ 1.0 与 equity_dd ≤ 10%；mem.max_drawdown 保留为辅助门槛（单笔亏损 ≤15%）；
  3) mem.sharpe_ratio 保留（20+ 消费点不动），dashboard 更名“胜利信号EMA”展示；
     strategy_learning_service 晋升条件同步改用 real_sharpe；
  4) 收益率口径：_compute_paper_total_return_pct 分母改【时间加权平均占用保证金】
     = Σ(margin_i×持仓时长_i)/总时长（margin=entry×qty/leverage），回测侧 backtest_return_pct 同口径重算；
     无 margin 数据时回退【起始资金+净入金】口径并标注 return_basis。
- 迁移：alembic 新增列；旧策略记忆 backfill 一次（用 strategy_trades 重算）。
- 测试：构造 30 笔交易序列（含分批平仓、多空混合），断言三指标与手算一致。
- 验收：Gate2 报告显示 real_sharpe/equity_dd/return_basis 三口径。

#### P0-4 决策快照精确关联（decision_id 端到端）
- 问题：平仓回写按 symbol+pnl NULL+48h 最近一条匹配；写入端 120s 去重丢快照 → 归因错配。
- 根因：R2（无唯一关联键）+ R5（模糊匹配把“找不到”静默变成“找错的”）。
- 影响面：【待并入代理结果：DecisionSnapshot 消费链清单】
- 设计（草案）：
  1) decision_snapshot_writer 生成 snapshot_id（uuid），写入 DecisionSnapshot 行并在
     开仓决策上下文（position.metadata / StrategyTrade.decision_context）保存；
  2) 平仓回写优先取 decision_context 的 snapshot_id 精确匹配；
     回退路径降级为“宁缺勿错”：仅当 symbol+strategy+方向一致且窗口内唯一时才匹配，
     否则写 attribution_ambiguous 事件；
  3) 去重规则改为：同 signature 窗口内重复合并写一次但记录 skipped_count，不再静默丢弃
     （快照保留并标记 duplicate_of）。
- 迁移：旧快照无 snapshot_id 的按 symbol+ts+strategy+action 生成稳定 hash id。
- 测试：同币同 strategy 48h 内两次开平回放，断言两笔 pnl 各自挂到正确快照。
- 验收：attribution 对拍脚本 100% 匹配；ambiguous 事件可观测。

#### P0-5 前视修复（回放 + 回测 + 读取层）
- 问题：live_pipeline_backtest_engine:654-670 最近邻取未来费率/FGI；backtest_engine fill_model 默认 close；
  forming bar 被当已收盘消费。
- 根因：R4。
- 影响面：LivePipelineBacktestEngine 被 strategy_evolver / strategy_params_registry /
  qaa_evolution_bridge / parity_score 消费（进化与参数校验路径）；data_center.get_klines 是全部指标/因子读入口。
- 设计：
  1) _get_funding_rate/_get_fgi 改【只取 ≤ ts 的最近样本】（direction=backward），超 max_age 返回保守默认值；
  2) backtest_engine：fill_model 默认改 next_open（env BACKTEST_FILL_MODEL 默认 next_open），close 仅显式 opt-in；
     内部调用点逐一核对并标注成交口径；历史回测重跑对比，量化 close→next_open 收益落差；
  3) data_center：get_klines/_query_best_exchange 增加 closed_only 参数，
     按 timestamp+period 与 now 判断 forming bar 并剔除；purpose=trade 默认 closed_only=True，
     research 默认 False；KlineResult 增加 closed_only 标记；
  4) 消费侧批量排查：决策路径（factors/indicators/signal）确认传 closed_only=True。
- 测试：决策点恰在样本间隙的用例断言取不到未来样本；回放 vs 实盘对拍脚本一致率 100%。
- 验收：回放对拍一致率 100%；回测收益与实盘偏离收窄。

#### P0-6 PnL 双计修复（三处）
- 问题：learning_loop_service._paper_position_pnl(526-537)、reentry_cooldown._durable_reopen_blocked(394-396)
  重复累加 partial_realized_pnl。
- 根因：R5（partial 的含/不含语义无权威定义）。
- 设计：
  1) 权威语义写进 paper_trading_engine.close_position 注释：closed 状态下 unrealized_pnl = 全仓已实现盈亏（含分批）；
  2) _paper_position_pnl：直接取 pos.unrealized_pnl；仅当其为 0/None 且存在 close_price 时才兜底重算；
  3) _durable_reopen_blocked：pnl = unrealized_pnl（删除 +partial 行）；
  4) 全库 grep partial_realized_pnl 其余消费点逐一核对口径并加注释。
- 测试：构造“TP1 减 25% 盈利 + 尾仓小亏平仓”用例，断言学习侧 pnl == 引擎 total_pnl，
  且耐久冷却判定为亏损（修复前为盈利）。
- 验收：三分一致对拍断言（引擎 == 学习 == 冷却）落地为单测。

#### P0-7 出场执行修复（TP3 方向 / 锁利单调 / 紧急 SL）
- 问题：paper_trading_engine.py:2459 TP3 空单追踪漏乘 side_dir；profit_drawdown_guard 锁利 SL 非单调；
  unified_exit_executor._set_emergency_sl 固定 5%。
- 根因：R5（多空分支手工对称易漏乘；收紧无单调不变量；紧急 SL 无杠杆感知）。
- 影响面：所有空单分段止盈、所有回撤锁利仓、所有无 SL 被硬闸门转设紧急 SL 的仓。
- 设计：
  1) TP3：_new_sl = _peak_price + _atr_price × trail_mult × _side_dir（与 TP1/TP2 对齐），
     抽公共函数 _trail_sl_from_peak(pos, atr_price, mult) 消除漏乘类错误；
  2) 锁利单调：_tighten_sl_unified 与引擎应用 _dd_action[new_sl] 两处统一加 apply_sl_monotonic(pos, new_sl)：
     long 只允许 new>old、short 只允许 new<old，违反时取原值并记 warning；
  3) 紧急 SL：_set_emergency_sl 杠杆感知——d = min(0.05, max(0.5/leverage, 0.01)) 或直接读 pos.liquidation_price，
     设 SL 后调用 paper_engine._ensure_sl_inside_liq 校验（复用现成函数），
     硬约束“SL 在爆仓价内侧且 ≥0.5%×entry 缓冲”；
  4) 结构性加固：update_position_tp_sl 内部加 _ensure_sl_inside_liq（所有 AI 调 SL 都受保护）。
- 测试：空单 TP3 断言 SL 在现价上方；锁利回撤序列断言 SL 单调；20x 紧急 SL 断言在爆仓价内侧。
- 验收：三类单测 + 空单分段止盈回放对拍。

#### P0-8 资金费结算默认开启
- 问题：_maybe_settle_funding 仅 research 档生效（settings 默认 demo）→ 中期持仓 PnL 无资金费。
- 根因：R5（功能随 research 模式上线后未推广到默认档）。
- 影响面：全部纸面盈亏 → StrategyMemory/Gate2/学习闭环；与 funding_net_rr_ok 入场闸门口径矛盾
  （midlong_trade_design.py:164-189 按费率估算成本拦单，成本在 PnL 中永不兑现）。
- 设计：
  1) 新增配置 FUNDING_SETTLE_ENABLED（默认 true，env_registry 注册），
     _maybe_settle_funding（4258-4260）改为 if not FUNDING_SETTLE_ENABLED: return，移除 tier 门；
     调用点有两处：update_single_position:3975 与 update_all_positions:4171，两处都受益；
  2) 复用 PaperFundingLedger（字段已齐），demo 档写入同一张表，新增可空 tier 列区分来源；
  3) 结算时刻对齐固定 8h UTC 边界（00/08/16），修复“距上次结算”带来的相位漂移；
  4) 分两步上线：先只记账不动净值（dry-run 两周）→ 再计入 realized_pnl。
- 迁移：tier 列可空新增（alembic）。
- 测试：跨 8h 边界持仓 20h 断言结算 2 次且方向正确。
- 验收：dry-run 报告与实盘资金费对拍误差 <5%。

#### P0-9 AI 因子发现走候选态 + allowlist 强制
- 问题：ai_factor_discovery 直接写 .py + hot_reload 进生产 FACTORS（startup.py:374-389 每 10 分钟跑一次）；
  多条信号路径 compute_all_factors 不传 allowlist（ai_decision_integration:74/400、signal_bus:238/308、
  v3_factor_pipeline:255、strategy_coordinator:376）。
- 根因：R2（发现→验证→晋升三段式只建了发现，验证段被绕过）。
- 设计：
  1) run_discovery_cycle 不再直接 hot_reload：写 ai_generated 后先经 factor_backtest_scorer.score_formula
     跑 OOS 回测（P0-1 修复后的 DSR/PBO 闸门），通过者登记 candidate，再走 active_set_policy A/B 晋升；
  2) 全部实盘信号路径 compute_all_factors 显式传 allowlist（由 scalp/midlong active set 提供）；
     运行期断言：purpose=trade 且 allowlist 为 None 时 warn 并默认用 active set；
  3) hot_reload 仅保留运维手动场景（加 audit 日志）。
- 迁移：现有 22 个 ai_generated 因子回溯评估，按结果进候选/归档。
- 测试：mock LLM 产出前视作弊因子，断言被候选闸门拒绝。

### P1 组（闭环正确性）

#### P1-1 晋升收益率口径统一 → 并入 P0-3 第 4 点。
#### P1-2 降级策略粒度与判据
- 设计：删会话级 trading_mode 翻转，改 per-strategy live_stage→none（字段已有）；
  判据改期望值类：win_rate×avg_profit+(1-win_rate)×avg_loss < 0 且样本≥30，
  或 equity_dd>18%（P0-3 新指标），或 mc_ratio>0.55（保留）。
#### P1-3 DRL 重训触发接线
- 设计：check_and_coordinate 恢复 _should_retrain_drl 判定并设 action.trigger_drl_retrain；
  learning_loop_service:748 分支已存在；drl_train_job.run_shadow_training 与
  SystemCoordinatorState.last_drl_training_at 冷却字段已就绪（drl_train_job.py:150-164）。
#### P1-4 live 反馈 7 天幂等补扫
- 设计：_do_outcome_batch cutoff 从 600s 扩到 7 天，依赖 decision_context._learning_loop_processed 幂等标记；
  limit 500 改 closed_at 游标分页。
#### P1-5 连亏/漂移状态落库
- 设计：_loss_streaks/_trade_counters 写入 SystemCoordinatorState JSON 列；
  concept_drift pnl 序列带时间戳落库（新表 drift_pnl_series，滚动裁剪），重启恢复。
#### P1-6 RL sizer 集成修复
- 设计：按 rl_position_sizer 真实接口修正 trading_decision_interface.py:166-174；
  异常升级为 warning，加“RL 建议命中率”监控指标。
#### P1-7 tier→nature 单一权威
- 设计：全库统一 import tp_sl_authority.TIER_TO_NATURE；删除 unified_learning_service:27 本地映射；
  trade_nature_resolver 改薄封装（内部委托）或全量替换后删除。
#### P1-8 分段止盈统一
- 设计：以 _run_unified_staged_tp（落库可恢复）为唯一权威；long_tier_staged_tp 与 nature_staged_tp 退役，
  LLM tp_stages_override 能力迁移到统一实现；跳档修复：TP3 触发前先补齐 TP1/TP2 减仓至设计比例。
  （已定稿：见第 8 节 P0-10/P1-8）
#### P1-9 walk_forward 年化参数化
- 设计：_calc_sharpe 增加 periods_per_year 参数；明确 validate() 生产接线策略
  （接入 Gate2 附加门槛，或显式标注仅人工使用）。
#### P1-10 因子前视 AST 审计（冷池 + active + ai_generated 全量）
- 设计：正则升级 AST 审计（ast.parse 遍历 shift 调用参数，静态求值为负常数即拦截；变量型标记人工复核），
  覆盖冷池扫描、active 集、AI 注入三个入口。
#### P1-11 learned_weighting 训练卫生
- 设计：train() 强制时间切分（前 80% fit、后 20% 校验，校验 IC 不达标丢弃）；
  purge_bars embargo 生效；min_ic_to_include 特征筛选生效；到期未重训告警并记录 last_effective_train。
#### P1-12 标签与记忆卫生
- 设计：regime_at_exit 平仓时回读当前 regime；cycle_direction_probability 的 range_thr 只用训练段；
  教训时间戳统一为 ts 字段（补齐 discovered_at 与同步点），memory_decay 兼容多键、无时间戳默认 7 天过期。

### P2 组（健壮性/清理）

#### P2-1 price_cache 交易所键：legacy 接口增加 exchange 参数纳入 key，7 处调用点逐一补传。
#### P2-2 forming bar 读隔离：见 P0-5 第 3 点（closed_only）。
#### P2-3 维护保证金按交易所：_calc_liquidation_price 增加 exchange 参数，调 fee_schedule_service.get_maint_margin_rate。
#### P2-4 paper 限价单 maker/taker 修正：可市价化限价单按 taker（对侧盘口价成交、收 taker 费），仅 resting 按 maker。
#### P2-5 死代码清理：backend/factor_engine/ 整目录归档；scalp_exit_override 残留确认后删除或补源码；
  entry_confidence_gate 死分支；learned_weighting 死配置（P1-11 激活或删除）；trade_nature_resolver 第三映射；walk_forward 接线决策。
#### P2-6 A/B 框架统计修正：ttest_rel → Welch t 检验（当前未接线，修正后仍默认关闭）。
#### P2-7 冷池变体择优多重比较标注：固定前瞻期或 Bonferroni/DSR 校正，报告标注 selected_fwd。

---


## 6. 根因追踪补充（晋升闸门端到端，已核实）

### 6.1 因子晋升判定链（score_formula → active）
- factor_backtest_scorer.score_formula:422-469：dsr_required 默认 True（:320）→ _dsr_pbo_gate →
  评级（A: |IC|≥0.05 & ICIR>0.5 & perf_ok；B: ≥0.03 & >0.3；admitted=A/B）→ validate_and_promote:547
  写 status=active 并热加载。
- _dsr_pbo_gate:485-505 双重 fail-open：跨币样本 < DSR_MIN_SYMBOLS(=4) → return (True, None) 放行；
  即便样本够，PBO 由按值分组的伪 CSCV 计算 ≈0 → pbo<=0.5 恒过。dsr_required 闸门在默认部署（3 币）下形同虚设。
- 三条调用路径对 indeterminate 语义分裂：factor_backtest_scorer（fail-open，唯一 active 晋升入口）；
  purge_pipeline.py:126（fail-closed 全拒）；factor_evolution_loop.py:1309-1310（直接读字段，
  lifecycle.py:117 pbo<=0.35 兜底拒）——同一概念三种语义，P0-1 必须统一为“样本不足一律 fail-closed + 显式 skipped_reason”。
- 实际晋升入口：factor_discovery.py:353 / factor_jobs.py / factor_sync_routes.py:326 / alpha101_factors.py:137
  → validate_and_promote（公式因子实盘生效路径）；factor_evolution_loop → ShadowJudge/lifecycle 落 factor_active_rows
  （进化因子另一存储与闸门）；ai_factor_discovery_service.py:282-292 绕过 score_formula 直接注入+hot_reload。
- 进化回测同样前视：strategy_evolver.py:340 用 LivePipelineBacktestEngine，
  live_pipeline_backtest_engine.py:432 entry = bar.c×(1±SLIPPAGE) 同根收盘成交；
  backtest_engine.fill_model 的 close 默认甚至不是进化路径的主引擎 → P0-5 的 next_open 修复必须同时覆盖
  LivePipelineBacktestEngine 的入场价模型（信号在 bar i 收盘产生时，成交价应为下一根开盘，或明确采用保守滑点模型）。
- factor_evaluation_pipeline 是【实盘信号加权层】而非报表：scalp_loop.py:382 / scalp_factor_router.py:419
  调 compute_weighted_signals；:220 的 decay penalty 在实盘信号生成时生效 → P0-2 接线后即对实盘信号起作用。

### 6.2 出场链补充（已核实）
- _tighten_sl_unified(2524-2543) 本身带单调守卫（long 只升、short 只降），TP3 空单 bug 根因是
  【输入漏乘 side_dir】（2459 行），修一处即可；但 profit_drawdown_guard 的三处应用（2760/2773/2784）
  是直接赋值不走该守卫 → P0-7 的 apply_sl_monotonic 应复用 _tighten_sl_unified 而非新增逻辑。
- _peak_price_from_pos(312-324)：short 峰值 = entry×(1-peak_pnl_pct)（在入场价下方），
  证实 TP3 空单 _new_sl=peak-Δ 落在现价下方 → 修复式 _new_sl = _peak_price + Δ×_side_dir 正确。
- open 仓 unrealized_pnl 每 tick 重算（3965-3968），closed 仓 unrealized_pnl=含分批的 total_pnl（1533），
  两种语义并存是双计 bug 的根源 → P0-6 的“权威语义注释 + 消费端修正”设计成立。
- 资金费：_maybe_settle_funding 由 update_single_position:3975 在持仓保护循环调用；
  PaperFundingLedger 字段已齐（account/position/symbol/side/notional/rate/payment/settled_at）；
  funding_net_rr_ok（midlong_trade_design.py:164-189）按 hold_hours/8h 估算成本拦单 → P0-8 开关化设计成立。


## 7. P0-1/P0-5/P0-9 设计定稿（晋升闸门代理 fix_points 并入）

### P0-1 定稿：DSR/PBO 多重检验闸门统一
1) compute_pbo_simple 改为接收 N×T 时序 IC 矩阵（因子×时间段），按时间轴切 S 片做真 CSCV；
   禁止对跨币均值标量 argsort（dsr_pbo.py:169-171 重写）。
2) 样本不足语义统一为 fail-closed + 人工复核队列：
   - factor_backtest_scorer._dsr_pbo_gate:485-505：MIN_SYMBOLS 默认改 3（与 FACTOR_SCORER_SYMBOLS=BTC,ETH,SOL 对齐），
     不足或 indeterminate 时 return (False, 0.5) 并写 skipped_reason（现为 return (True, None) 放行）；
   - purge_pipeline.py:121-126 保持 fail-closed（已一致）；
   - factor_evolution_loop.py:1309-1310：改显式读 overall_passes 与 indeterminate，
     删除 .get(...,True)/.get(...,0.3) 放行缺省，任一不可判定即 fail-closed。
3) n_trials 动态化：n_trials = max(候选池实际数量, 已 active 因子数)，删除固定 40 默认（factor_backtest_scorer.py:424）。
   另核对 factor_evolution_loop.py:1136 的 ShadowJudge 独立阈值（pbo>0.30 拒绝）与新统一口径对齐，避免第三套阈值。
4) dsr_pbo.py:266-269 的 overall_passes 计算保持，作为三条路径的【唯一】判定出口。

### P0-5 增补：进化回测的成交价模型
- 进化路径不走 backtest_engine 的 fill_model（strategy_evolver.py:340 用 LivePipelineBacktestEngine），
  其 entry=bar.c×(1±SLIPPAGE)（live_pipeline_backtest_engine.py:432）为同根收盘成交前视。
- 修复：LivePipelineBacktestEngine 开仓价改为下一根开盘（entry=next_bar.o×(1±SLIPPAGE)，对信号 shift(1)），
  或提供 fill_model 参数（close/next_open，默认 next_open）；与 funding/FGI 的 backward 取值一并交付。
- backtest_engine.BacktestConfig.fill_model 默认 close→next_open 不变，另补向量化模式按 next_open 计价。

### P0-9 定稿：AI 因子发现候选态
- 已证实 ai_factor_discovery_service.py:282-292 无任何 score_formula/validate_and_promote 引用：
  inject_factor 写 .py 后直接 factor_engine.hot_reload() 进注册表。
- 修复：inject_factor 后不再 hot_reload；改登记 custom_factor_store 候选并调
  factor_backtest_scorer.validate_and_promote，A/B 级才 _load_active_custom_factors（与
  factor_cleanup_service.py:207 的唯一晋升入口口径一致）。
- factor_evaluation_pipeline 确认是实盘信号加权层（scalp_loop.py:382 / scalp_factor_router.py:419），
  晋升闸门只有两条链：score_formula/validate_and_promote（公式因子）与 factor_evolution_loop+lifecycle（进化因子）。


## 8. 出场链定稿（代理 fix_points 并入）与【双重减仓】升级

### P0-10（新增）默认配置下两套分段止盈引擎并行 → long 仓双重减仓
- 已证实：RISK_V2_UNIFIED_STAGED_TP=true（settings.py:1816）且 RISK_USE_LONG_TIER_STAGED_TP=true 同时默认开。
  ① paper 引擎 _run_v2_protection→_run_unified_staged_tp（paper_trading_engine.py:2938 调用点；scheduler.py:650 每第 10 tick≈30s，全层级）；
  ② mlto_cycle→manage_position→_dim_staged_tp（midlong_position_manager.py:255/643，仅 long 层），
  后者只查 tier=="long" + RISK_USE_LONG_TIER_STAGED_TP，【没有 v2 门】→ 同一 long 仓可能被两条路径各自减仓；
  ③ PEO 的 nature_staged_tp 在 v2 开启时被强制 hold（position_exit_orchestrator.py:102）。
- 修复设计：二选一收口。方案 A（推荐）：RISK_USE_LONG_TIER_STAGED_TP 默认 false，
  _dim_staged_tp 调用点加 v2 门（RISK_V2_UNIFIED_STAGED_TP 开启时直接跳过），统一走落库的 tp_level_reached；
  方案 B：保留 long_tier 路径但先补齐 StagedTpState 持久化并加互斥锁。
- 迁移：改开关默认值 + 一次回归对拍（对比收口前后 long 仓减仓次数分布）。

### P0-7 定稿细化（出场执行）
- TP3 空单修复式：_new_sl = _peak_price - _atr_price × trail_mult × _side_dir（paper_trading_engine.py:2459；
  与 TP1/TP2 的 side_dir 写法一致；2519 行同型复核）；_peak_price_from_pos 语义已确认正确，无需改。
- 锁利单调落点：engine 层 4 处直接赋值（paper_trading_engine.py:2713/2759/2771/2783）统一改走
  _tighten_sl_unified（2524-2543 已带单调守卫），不再直接写 pos.sl_price。
- 紧急 SL：触发链为 check_sl_v6_hard_block(unified_exit_executor.py:159 convert_to_set_sl)→:386 分发→
  _set_emergency_sl(:399-403 固定 5%)；第二入口 master_execution.py:2190 同样调用 _set_emergency_sl。
  修复：_set_emergency_sl 按杠杆/liq 计算（SL = liq ± entry×安全边距），取 ORM 对象调
  paper_engine._ensure_sl_inside_liq(440-484) 钳到 liq 内侧，再 update_position_tp_sl；
  update_position_tp_sl(3914-3947) 内部补同一守卫（覆盖 AI 主动调 SL 路径）。
- 跳档修复（P1-8 升级到 P0-7 一起做）：2452-2483 的 if/elif 链在 TP3 触发时先补齐未触发的 TP1/TP2 减仓
  （累计至设计比例 80%）再置 tp_level_reached=3。

### P0-8 定稿细化（资金费）
- 最小改动：新增 FUNDING_SETTLE_ENABLED（默认 true）替换 PAPER_SIMULATION_TIER=="research" 门（4258-4260），
  保留 FUNDING_SETTLE_INTERVAL_SEC 8h 节流（与 funding_net_rr_ok 的 8h/0.01% 假设一致）；
  PaperFundingLedger 复用，加可空 tier 列。

### P1-8 更新：分段止盈统一（在 P0-10 收口后执行）
- 以统一版为唯一权威；long_tier_staged_tp 退役：StagedTpState 无序列化的缺口（long_tier_staged_tp.py:29-36）
  不再补（方案 A 下直接下线）；nature_staged_tp 的 LLM tp_stages_override 能力迁入统一实现参数；
  PEO 在 v2 开启时的 hold 旁路（position_exit_orchestrator.py:102-103）保留但加注释说明职责边界。


## 9. P0-4 定稿（决策快照精确关联）与 P1-4 修订（live 兜底死路）

### P0-4 决策快照精确关联（已核实的缺口与落点）
- 已核实：DecisionSnapshot 已有 proposal_id/trace_id 列（models.py:3134-3135，master_execution.py:1329-1330 生成），
  但从未回传 PaperPosition/StrategyTrade；unified_learning_service.py:559 已有 snapshot_id 拷贝键（decision_context），
  paper/live 平仓路径不传该值；PaperPosition 无 metadata_json 列（paper_trading_engine.py:3307 的 getattr 恒返回 None）。
- 修复设计（两阶段链接，无需等开仓时就有仓位 id）：
  1) 写入端：_dedup_signature（decision_snapshot_writer.py:25-36）加入 strategy_id 与 trace_id；
     _dedup_check 对 executed 快照不做去重（41 行已有 id 判定，扩展到 executed 标记）；
     去重不再静默：同签名丢弃记 skipped_count（保留第一条并标记 duplicate_of）。
  2) 开仓链接：_mark_master_decision_executed（full_auto_trading_service.py:2427-2451）在下单成功回写 executed 时，
     同时把 pos_id 写入 snapshot（新增可空列 position_id，alembic）；
     若该时刻仓位 id 尚不可得，则由保护循环首次见到该仓位时做【回溯链接】：
     按 (strategy_id, symbol, 方向一致, executed=true, position_id is null, ts 最近 10min) 唯一匹配则链接，
     不唯一则写 attribution_ambiguous 事件。
  3) 平仓回写：_write_retrospective（paper_trading_engine.py:3469-3486）优先按 pos 上的 snapshot 链接精确匹配；
     回退路径收紧为：symbol+strategy+方向一致+pnl NULL+48h 且【唯一】时才匹配；否则只记 ambiguous 不写。
  4) 学习侧：close 时 outcome.metadata 传 snapshot_id（unified_learning_service.py:559 的拷贝键已就绪）。
- 消费链污染落点（已核实 6 条，修复后需回归）：experience_retriever(:197/228)、rag_knowledge_service._build_trade_decisions
  （:454/494，trade_decisions 向量库）、local_llm/dataset_builder（:92，SFT 数据集 pnl_pct 作监督标签）、
  evolution_scheduler.weekly_experience_distill(:649)、trade_attribution_service(:122)、block_pattern_learning（间接）。
- 消费端短期防御（上游修复合入前先上）：各查询追加 DecisionSnapshot.proposal_id.isnot(None) 或
  (strategy_id, symbol, executed) 校验，避免把 pnl 为空/错配行纳入统计与训练。

### P1-4 修订：live 反馈兜底是死路 → 改为仓位级 7 天补扫
- 已核实：_do_outcome_batch 只扫 StrategyTrade，而 _persist_strategy_trade 恒写
  _learning_loop_processed=true（unified_learning_service.py:571）→ 事件级 hook 失败时根本没有 StrategyTrade 行
  可扫，600s 窗口形同虚设；仅 paper 有 _do_paper_outcome_backfill(days=7) 仓位级补扫。
- 修复设计：把仓位级补扫推广到 live：_do_paper_outcome_backfill 改为扫描全部 closed PaperPosition
  （按 mode/source 字段区分 paper/live，需先确认 live 仓位是否也落 PaperPosition 表；若在独立表则同样加补扫），
  幂等键沿用 decision_context 中的 paper_position_id 去重（learning_loop_service.py:577-586 已实现）；
  同时修复根因：close hook 中 process_outcome 失败时也要落一条 unprocessed 的 StrategyTrade（persist 先行、
  学习后行，processed 标记在 process_outcome 成功后才置 true），使 600s 兜底真正有东西可扫。

### P1-12 补充：教训时间戳统一（已核实键名清单）
- memory_decay_service.py:221/256 只认 ts；trade_memory_miner 写 discovered_at；lesson_utils/opencode 用 ingested_at；
  pattern_extractor 用 extracted_at；decision_feedback_service.py:889-897 的 sync_lesson_to_strategy_memory 与
  evolution_scheduler.py:678-704 周提炼完全不写时间戳 → 永不衰减。
- 修复设计：统一写入端以 ts 为准（各写入点补 ts=now），memory_decay 兼容识别 discovered_at/ingested_at/extracted_at，
  无任何时间戳的条目按“入库时间”补一次迁移，之后默认 7 天过期策略。


## 10. P0-4/P1-4 最终定稿（快照关联键与 live 补扫数据源）

### 关联键落点（全部已核实）
- 关键事实：PaperPosition 已有 exit_state_json Text 列（models.py:2467），无需新增列即可承载决策关联——
  开仓时把 proposal_id/trace_id 写入 exit_state_json（与 nature_staged_tp 的持久化同列、同机制）。
- 写入端唯一 hook：master_execution.py:1329-1336（DecisionSnapshot.build 与开仓执行同作用域，
  _prop.proposal_id/_prop.trace_id 可用）——在该处把这两个 id 写进新建 PaperPosition 的 exit_state_json。
- close_position 签名（paper_trading_engine.py:1411）：无需新增参数，从 pos.exit_state_json 读 decision_id；
  平仓回写（3469-3486）优先 DecisionSnapshot.proposal_id == pos.decision_id 精确匹配。
- 学习侧：TradeOutcome.metadata 增加 snapshot_id，写入点两处——paper 平仓路径 paper_trading_engine.py:3304-3370、
  live 平仓路径 trading_commands.py:1894-1920；unified_learning_service.py:552-568 的 snapshot_id 拷贝键
  自动落 decision_context → (snapshot, trade, position) 三方唯一关联闭环。
- StrategyTrade↔PaperPosition 的关联键 decision_context.paper_position_id 已存在（unified_learning_service.py:456-476 去重用它），
  缺的只是 DecisionSnapshot↔PaperPosition 这半条边，补上即完整。

### live 补扫数据源（P1-4 定稿）
- live 仓位级 7 天补扫的数据源：AIDecisionLog.realized_pnl + pnl_updated_at（或 HyperliquidTrade 聚合），
  按 decision_log_id 去重；仿 _do_paper_outcome_backfill(days=7) 实现 _do_live_outcome_backfill。
- _do_outcome_batch 的 window_s（learning_loop_service.py:356）从 max(interval×2,300) 扩到 7 天，
  并改 closed_at 游标分页（现有 order desc + limit 500 会把未补扫旧单持续挤出）。
- 根因侧修复：close hook 中 persist 先行（落未 processed 的 StrategyTrade）、学习后行，
  processed 标记在 process_outcome 成功后才置 true——让 600s 兜底真正有东西可扫。

## 11. 落地顺序与里程碑

- M0（1~3 天，资金安全 P0）：P0-6（PnL 双计）、P0-7（TP3 方向/锁利单调/紧急 SL/跳档补齐）、P0-8（资金费）、
  P0-10（双重减仓收口：RISK_USE_LONG_TIER_STAGED_TP 默认 false + v2 门）→ 只改记账与风控，不动闭环。
- M1（3~7 天，闭环自证 P0）：P0-5（前视）、P0-3（指标口径）、P0-4（快照关联）、P0-1（DSR/PBO）、P0-2（衰减接线）、P0-9（AI 因子候选态）。
- M2（1~2 周，P1 闭环正确性）：P1-1~P1-12。
- M3（2~3 周，P2 清理）：P2-1~P2-7。
- 每里程碑后跑回归对拍：(a) 回放 vs 实盘决策一致率；(b) 晋升候选修复前后变化审计；(c) 纸面净值与交易所对账单差异报告。

## 12. 验收总纲（负面测试优先）

1. 每项修复必须配构造性反例测试：先写能复现 bug 的用例（红），修后转绿。
2. 前视类修复统一用置换/时间反转测试：把未来样本搬走或时间反转，输出必须改变且方向符合预期。
3. 记账类修复用对拍断言：引擎 total_pnl == 学习侧 pnl == 冷却侧 pnl（三分一致）。
4. 接线类修复用调用点断言：grep 断言目标函数存在非测试调用点且被调度任务引用。
5. 晋升口径修复用影子评估：新口径先 shadow 跑两周，对比偏差报告后再切换。

## 13. 风险与回滚

- 所有 P0 修改以 feature flag 包裹（FACTOR_SCORER_PBO_V2、PAPER_FUNDING_SETTLE_ENABLED、BACKTEST_FILL_MODEL、closed_only 参数化），
  默认新行为、可一键回旧。
- 晋升相关修改（P0-3/P0-4/P1-1/P1-2）先 fail-closed：不确定时拒绝晋升而非放行。
- 历史数据不做批量改写（PnL 历史保留原口径并打口径标记），避免污染既有统计。
