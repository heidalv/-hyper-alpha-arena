# 完成反馈：中长线改造 阶段0+1（2026-08-14）

> 生成时间：2026-08-14 上午 · 依据：git log、`docs/中长线改造_测试验证报告_2026-08-14.md`、`data/discovered_factors.json`、后端日志与健康检查
> 本文件为会话级总结的磁盘持久化副本，避免"会话结束后收不到反馈"的问题。

## 一、总体结论

阶段0（F1-F8）+ 阶段1（P0-A / P0-G / P0-D / P0-E）**全部完成并通过验证**；后端已于 08:30 重启，健康检查通过（healthy, v0.7.0），代码改动已生效。阶段2-1 因子挖掘第一轮跑通，但 **23 个候选全部未达标（rejected）**。

## 二、交付清单（全部独立 commit，可回滚）

| Commit | 内容 |
|---|---|
| `1b85b06` | 阶段0 F1-F7 + P0-A（catalog 黑名单、选币 fail-closed、中线宇宙防线、模板晋升门禁、周报租户上下文、砍仓硬门、SwingAgent 回退禁开仓、pair_research 关停） |
| `1223f0a` | P0-G 模板白名单治理（停用 16 reversion/占位/零回测 + 3 低样本晋升，迁移含 downgrade） |
| `c1834e6` | F8 前端 TierInfo 契约修正 |
| `04b5150` | P0-D 冷却/门禁透明化（`get_cooldown_snapshot` + `/cooldowns/{sid}` + `/events/{sid}` + 仪表盘冷却矩阵/拦截流） |
| `e9a8f78` | P0-E 分层熔断（周期级独立日亏预算、币种冻结 tier 归因、修复 `sa_text` NameError） |
| `751e8ce` | 阶段2 因子管道 tenant context 丢失修复（ContextVar None → 显式解析 admin tenant） |
| `c370e90` | 因子 Ops 控制台 midlong parity（OpsMidlongFactors 面板 + /ops/midlong-factors、/mine、/prune） |
| `9b1bc02` | ops midlong preflight kline_service 导入路径修复 |

## 三、验证结果

- `compileall backend` ✅ 0 错误
- `check_config_drift.py` ✅ NO DRIFT
- 前端 `tsc --noEmit` ✅ 0 错误；`next build` ✅ 26 路由全部静态预渲染
- 定向单测 ✅ 77 passed / 3 failed（3 败为基线预存在，非本批引入）
- 数据对账 ✅ `paper_orders sum(pnl)=-65.83` == `paper_balance realized=-65.83`（4673 笔 filled；权益 417.00；5 开仓；保证金 68.35）
- P0-D/P0-E 快照冒烟 ✅（当前无冷却；三周期熔断 unfrozen）
- F1 catalog ✅ 非加密行=0（二次清理后 2579 行）
- P0-G 白名单 ✅ active mid/long=10 个；reversion 家族=0

## 四、额外发现并修复的 3 个隐藏根因

1. **`sa_text` NameError**：per-symbol 日亏损追踪与冻结 Layer1 一直是死代码，已补导入（`e9a8f78`）。
2. **tier 归因优先级反转**：近 14 天 1756 笔订单中 1735 笔被误记 mid（短线仅 3 笔）；改为 trade_nature 优先后 short=1666/mid=72/long=18。
3. **旧进程重复重灌污染**：清理后运行中的旧进程数小时内把 AAPL/MSFT/NVDA/XAG/XPT/XPD/NG 重新写回 catalog 并重置止血配置；二次清理后重启，代码层唯一写入路径已被黑名单覆盖，不再复发。

## 五、后端重启（已完成）

- 08:30 重启（日志：`backend_restart_out_20260813b.log`），当前 `GET /api/health` → `healthy, v0.7.0`。
- 重启后生效：F1-F8 代码 + P0-A 两开关 + P0-D/P0-E 新接口与门禁。

## 六、阶段2-1 因子挖掘第一轮结果：23 候选全灭

- 候选数：23（alpha101 库，4h/1d 两周期）
- 结果：**rejected 23 / 晋级 0**，全部 grade C
- 关键样本（IC / OOS sharpe）：mom_4h -0.23/-2.06；mom_1d -0.13/-0.71；rev_z_4h +0.17/-1.70；alpha006_4h IC -0.07 但 OOS sharpe +0.87（被拒因 IC 门槛）；仅个别因子 IC 弱正
- 含义：IC 回测门槛下无一达标，需换因子库/参数域、扩样本或引入 WFO 后再筛；与验证报告 §5 "midlong 因子集 active=0" 的预期起点一致。

## 七、待用户拍板事项（原报告 §7）

1. 中线最终形态：选项 A（纯因子）/ 选项 B（因子路由+LLM 边缘裁决）
2. 阶段2-1 因子研究是否继续加大投入（当前第一轮 0 晋级）
3. 长线收敛 TrendAgent 唯一通道 + 提示词重构的确认

## 八、回滚与风险

- 代码：每步一个 commit，`git revert` 即可；数据：`postgres_backup/midlong_fixes_*` + P0-G 迁移 downgrade。
- 熔断只挡新开仓，不清仓、不强制平仓；Live 从不自动交易（live_sub_positions=0）。
