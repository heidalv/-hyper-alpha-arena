# 短线 + 中线合并审查报告

> 审查日：2026-08-20  
> 原则：**以当前仓库代码为准**。数字只标「仓库里最后一份报告」的日期，不当作今天实盘。配置只写「代码默认」或「事实清单曾登记」，仓库没有当前 `.env`，不当作此刻一定如此。  
> 不要再拿 2026-08-14 的《中长线亏损根因》《中线因子化评估》当现状——那些描述的是 SwingAgent / 意图转化 0%，和现码已经对不上。

---

## 0. 一句话

| 周期 | 现在怎么开仓（现码） | 核心问题 |
|------|----------------------|----------|
| **短线** | 独立循环 `scalp_loop` → 因子路由 → Gate → V5 → 下单 | 链路是活的；分数仍被当成「准」来加仓；总控仍能平/减短线仓；几处计算/缩仓会被自己抹掉 |
| **中线** | 独立循环 `midlong_loop` 在 `MIDLONG_MID_VIA_FACTOR_ROUTE=true` 时走 `factor_route`；两开关都关则前端显示「中线暂停」 | SwingAgent 已弃用；入场是 4h/1d 因子合成；出场仍大量走 LLM 复查 `trend_weaken/broken`；训练 K 线默认 Binance，成交跟活跃所 |

长线（`trend_follow` / `long_trend_v2`）不在本报告改造范围，只作对照。

---

## 1. 证据怎么用

| 类型 | 用来干什么 | 不能用来干什么 |
|------|------------|----------------|
| `backend/services/...` 现码 | 判定「现在逻辑是什么、有没有 bug」 | — |
| `reports/scalp_daily/scalp_health_2026-08-16.md` | 短线最后一份体检（信号/纸盘） | 不能写成「今天还是这些数字」 |
| `backend/data/midlong_reports/latest.md`（生成 2026-08-17） | 中线 swing / 长线 trend 最后一份周报 | 同上；且窗口内仍含因子路由上线前的 swing 单 |
| `docs/RUNTIME_CONFIG_FACTS.md`（2026-08-13） | 当时 `.env` 登记过什么 | 不能当此刻开关；和代码默认已有冲突 |
| 8 月 14 日前中长线设计稿 | 溯源「为什么做成现在这样」 | **不能当现行架构** |

---

## 2. 现行架构（只画现码）

### 2.1 短线（现码，未改道）

```
midlong 无关。入口：FullAutoTradingService → run_scalp_independent
  → 5m K 线（跟随活跃交易所，默认 asterdex）
  → compute_all_factors（可精选白名单）
  → scalp_factor_router.evaluate  或  regime=ranging 时 scalp_ranging_mr
  → scalp_execution_gate
  → evaluate_scalp_proposal（TCP/V5）
  → 可选 FlashVeto（约 35–44 分）
  → MTF / short_tier / 动态仓位 / EV 闸（代码默认开）
  → paper_engine.place_order(trade_nature=scalp)
```

Master 对短线：**只禁新开**（`SCALP_MASTER_HARD_BLOCK` 拦 buy/sell/pyramid/dca）。  
注释写「短线也跳过」，**后面没有 `continue`**，`close` / `reduce` 会继续往下走。减仓对 short 另有「浮亏 < 20% 不准减」。

### 2.2 中线（现码，8 月 15 日后）

```
midlong_loop.run_midlong_independent
  → 宇宙 = 固定 mid ∪ AI 中线候选(≤3) ∪ 已有 swing 续管
  → _run_mid = ("mid" in due) and 宇宙非空
  → _maintain_mlto_theses_for_session(run_mid=...)
       若 MIDLONG_MID_VIA_FACTOR_ROUTE：
         factor_route_decide → factor_route_open
           → execute_midlong_open(source=factor_route, nature=swing, tier=mid)
       若仅 MIDLONG_MID_VIA_MLTO：前端标「中线AI(过渡)」
       两开关都 false：前端 mid_mode=mid_paused，循环只占位 mid key
  → 出场：_run_midlong_active_exit + 模式 B manage_position
       （规则止盈止损 + LLM 复查 → trend_broken / trend_weaken）
```

**已废弃（测试仍在、主路径不再调）：** `swing_agent.analyze` 独立开仓。  
**不要再说：**「中线是 MLTO NIBBLE、转化率 0%」。2026-08-17 周报写的是 NIBBLE→24h 开仓转化率 **67.3%**，且入场主路径已是因子路由（开关打开时）。

代码默认：

| 开关 | 代码默认 | 含义 |
|------|----------|------|
| `MIDLONG_MID_VIA_MLTO` | **false** | 旧 AI 中线 thesis 开仓关 |
| `MIDLONG_MID_VIA_FACTOR_ROUTE` | **false** | 因子路由也关 → 默认「中线暂停」 |
| `MIDLONG_EXEC_AUTHORITY` | **mlto** | paper 下 `factor_route` 被当成合法 writer |
| `FACTOR_ROUTE_MIN_ACTIVE_FACTORS` | **2** | 活跃因子 <2 则 hold |
| `FACTOR_ROUTE_SL_PCT` / `TP` | **5% / 10%** | 固定比例，不是结构位 |
| `FACTOR_ROUTE_TRANCHE_MARGIN_PCT` | **0.12** | 小资金能过敞口闸 |

M4 实施记录（2026-08-16）写过：当时环境已用 5 个活跃因子投票、`factor_route` 在跑。那是当时机器实况，不是代码默认。

### 2.3 测试与现码打架（中线）

`tests/integration/test_swing_deprecated.py` 要求 `midlong_loop` 里 `_run_mid = False`。  
现码是 `_run_mid = ("mid" in due) and bool(_mid_universe)`，并在 `mlto_cycle` 里接了因子路由。  
**测试描述的是「中线并进长线 mid_view、不再调度中线」的中间态；现码已经把中线调度接回因子路由。** 这不是「中线又变回 SwingAgent」，是测试没跟上 8 月 15 日接线。

---

## 3. 短线缺陷（现码 + 带日期数据）

### 3.1 现码缺陷（今天打开文件仍在）

| ID | 严重度 | 位置 | 缺陷 |
|----|--------|------|------|
| S1 | P0 | `scalp_loop.py` 涨跌补数 | K 线已是 DataFrame，却用 `_klines[-1]` 当「最后一根」（取的是列名 -1）。异常被 `pass` 吃掉。`price_change_* <= 0` 把**真下跌**当成没数据。`classify_regime` 更容易误判震荡 → 走错 MR / 缩仓。 |
| S2 | P0 | `scalp_loop.py` FlashVeto 后 | `_size_mult = _liquidity_mult` 之后，否决「通过」会写成 `1.0`。周末/薄时段缩仓被抹掉。越弱、越走否决的单，反而可能满仓。 |
| S3 | P0 | 同文件预算 | `budget_service.scale_factor_for_layer(..., "paper")` 和 `can_open(..., "paper")` 写死。真金会话用模拟盘预算。 |
| S4 | P1 | `apply_short_tier_gate` | 不传 `mode`，`check_short_tier_entry` 默认 paper 冷却。 |
| S5 | P1 | `scalp_loop.py` 权益 | 余额行为空时 `sum(margin)*3` 估权益，后面按权益打仓会偏大。 |
| S6 | P1 | `scalp_ranging_mr.py` | `apply_learned_mr` 把学到的 SL 再夹到 `[1.2%, 2.0%]`。学到的 0.8% 会被抬宽。 |
| S7 | P1 | `scalp_score_calibration.py` | 高分历史不达标只封到 69。直通门槛约 45–50，仓位仍跟分数走。 |
| S8 | P0 | `master_execution.py` | 只禁短线新开。注释「也跳过」无 `continue`。`close`/`reduce` 仍能打到短线仓（减仓有 20% 浮亏门槛）。 |
| S9 | P1 | 多处 `except` | `short_tier` / 组合预算 / 仲裁 异常时跳过继续开（模拟盘组合预算默认 `PB_PAPER_SKIP=true` 整段不跑）。 |
| S10 | P1 | 震荡缩仓地板 | `gate.size_multiplier` 低于 0.999 时再 `max(0.70, ...)`。宇宙降级 0.35 会被抬到 0.70。 |
| S11 | P2 | 入场价 0 | `quantity = notional/entry if entry>0 else 0`，仍调用 `place_order`。 |
| S12 | P2 | 山寨过滤 | 看胜率 ≥42% + 笔数，不看平均净亏。胜率过线、期望为负的币仍可交易。 |

### 3.2 仓库最后一份短线体检（2026-08-16，不是今天）

`reports/scalp_daily/scalp_health_2026-08-16.md`：

- 已结算信号胜率 36.62%，平均净收益 -0.1946%；分数 ≥60 桶净收益 **-0.2408%**（高分更亏）。
- 纸盘已平 1481 笔，胜率 41.93%，利润因子 **0.95**，净亏约 -12.7 USD。
- 平仓：超时 346、选币移除 227、总控减仓 172、总控平仓 61、止损 135；止盈/保本是主要正贡献。
- Meta：`usable=False`，OOS AUC ≈0.52。
- 因子权重基尼约 0.068（几乎平均）。

**含义：** 到 8 月 16 日，分数仍不能当胜率用，但代码仍用分数乘仓位（`scalp_sizing` 的 `q_mult`）。8 月 17 日之后有没有好转，本仓库没有更新的短线体检。

### 3.3 配置：代码默认 ≠ 8 月 13 日清单

| 项 | 代码默认 | `RUNTIME_CONFIG_FACTS.md`（2026-08-13） |
|----|----------|------------------------------------------|
| `SCALP_EV_GATE_ENABLED` | **true** | **false** |
| `SCALP_EV_MIN_PCT` | 0.0003 | （清单未列） |

上次口头说「EV 闸运行配置是关的」——**说满了**。现码默认是开的；清单登记过关。没有当前 `.env`，不能判现场。

EV 闸即使开着：异常时模拟盘 fail-open；`p_win` 冷启动偏乐观；`SCALP_META_IN_EV` 默认关，且 meta 未 usable 时硬过滤不生效。

---

## 4. 中线缺陷（现码 + 带日期数据）

### 4.1 现码缺陷

| ID | 严重度 | 位置 | 缺陷 |
|----|--------|------|------|
| M1 | P0 | 入场 vs 出场两套脑子 | 入场：4h/1d 因子 z-score 投票。出场：模式 B 用 **LLM 方向复查** 打 `trend_broken` / `trend_weaken`。因子开的仓，被「趋势叙事」碎平。2026-08-17 周报 swing 平仓几乎全是这类文案。 |
| M2 | P0 | `has_open_midlong_position` | 同币只要有 **mid 或 long** 未平仓，因子路由就 `position_exists` 不开。中线和长线互锁，不是「同向中线最多 1 仓」。 |
| M3 | P0 | 训练所 ≠ 成交所 | `factor_route` 算因子走 `factor_backtest_scorer._load_klines`，默认交易所 **binance**（`FACTOR_BACKTEST_KLINE_EXCHANGE`）。成交跟会话活跃所（常见 asterdex）。同一套 MACD 在两个所上的 4h 不是同一条线。 |
| M4 | P0 | 固定 5%/10% 止盈止损 | `FACTOR_ROUTE_SL_PCT=0.05`、`TP=0.10`。不读结构、不读 learned、不读 ATR。10x 时 5% 价格止损 ≈ 保证金 50%。 |
| M5 | P1 | V5 日配额把 swing 归一成 trend | `normalize_v5_nature`：swing → trend_follow。中线和长线抢同一日开仓帽（清单登记 `TREND_DAILY_OPEN_CAP=15`）。 |
| M6 | P1 | 震荡仍可探针开仓 | `MIDLONG_ALLOW_RANGE_PROBE` 默认 true；paper 下 ranging → 缩仓仍开。周报大量「ranging 60% / ADX<20」后被复查打掉。 |
| M7 | P1 | 开关默认暂停 | 代码默认因子路由 **关**。未改 `.env` 时中线不入场，只跑研究。M4 记录的是当时环境已打开，不是仓库默认。 |
| M8 | P1 | 最少 2 个活跃因子 | 8 月 15 日验证报告曾只剩 macd@4h 一个 A 级 → 会 `insufficient_active`。M4 写到 5 个，复检仍可能掉回 <2。 |
| M9 | P1 | 负 IC 反手 | `orient = sign(ic_mean)`。活跃池里多个 A 级 IC 为负（M4：sma_cross@1d IC=-0.156）。合成方向是「做因子的反面」，解释性和稳定性都差，且高度依赖 IC 符号稳定。 |
| M10 | P1 | 新仓 LLM 复查 | 8 月 16 日已修「开仓 1–3 分钟就被 trend_broken」。现码：持仓未满 `MIDLONG_POSITION_MGMT_LLM_INTERVAL_SEC`（默认 900s）不复查。**过了 15 分钟仍用 LLM 叙事打因子仓。** |
| M11 | P2 | Paper 置信度不够可缩仓放行 | `evaluate_midlong_open`：paper + agent 独立路径，置信度过 base 就把 block 改成缩仓放行。因子路由若带上类似标记，门会变软。 |
| M12 | P2 | 测试过期 | `test_swing_deprecated` 断言 `_run_mid = False`，与现码冲突。 |
| M13 | P2 | mid_view 还在长线 thesis 里 | 长线 LLM 仍产出 mid_view；中线开仓已不靠它。两套「中周期看法」并存，容易看错监控。 |

### 4.2 仓库最后一份中线周报（2026-08-17，不是今天）

`backend/data/midlong_reports/latest.md`，窗口近 14 天：

| | swing（中线仓） | trend_follow（长线，对照） |
|--|-----------------|---------------------------|
| 平仓样本 | 96 | 21 |
| 窗口内开仓 | 47 | 5 |
| 胜率 | **16.7%** | 66.7% |
| 利润因子 | **0.32** | 9.28 |
| 净盈亏 | **-40.21** | +10.31 |
| 分档 TP L1+ | 几乎 0 | 0 |
| 可用定义 | 胜率/盈亏比 **FAIL** | 胜率项 PASS，样本不足 |

出场文案主体：`trend_weaken` / `trend_broken`（ADX 低、ranging 60%、多周期不共振）。  
同向再开率 58.3%（目标 ≤20%）。

**注意：** 这 14 天横跨因子路由上线日（8 月 15 日），不能把 -40 全部算到「当前因子路由」头上；但能说明 **出场仍是碎平结构**，和现码 M1 一致。

NIBBLE 转化率 67.3%——旧文档「253 次意图 0 成交」已过时，审查里不再当现行问题。

### 4.3 因子弹药（实施记录，2026-08-16）

M4：活跃池 0→5（`ai_gen_mrext@1d`、`sma_cross@1d`、`ai_a101_macd_sig_4h`、`ai_gen_vol_rev@4h`、`ai_gen_uncertainty_vola@1d`）。  
多数 IC 为负。8 月 15 日 2400 根复检时，曾只剩 macd@4h 稳健。  
**弹药薄 + 负 IC 反手 + 跨所 K 线**，是中线入场质量的现码风险，不是「没有因子路由」。

---

## 5. 两边相同的结构性问题

1. **入场分数/因子分没有被证明能预测盈亏，却驱动仓位或方向。** 短线：8 月 16 日分桶高分更亏。中线：z-score 合成未经与 swing 成交对账。  
2. **出场和入场不是同一套逻辑。** 短线：总控/超时/选币移除。中线：LLM 趋势复查打因子仓。  
3. **模拟盘门比真金松，且有的地方连 mode 都传错。** 短线预算写死 paper；中线 ranging 探针默认开。  
4. **账本/报告滞后。** 短线体检停在 8 月 16 日；中线周报 8 月 17 日。没有「今天」数字。  
5. **配置三套嘴：** 代码默认、事实清单、某台机器 `.env`（M4）。审查只认代码默认 + 清单曾登记。

---

## 6. 明确不再当问题的（避免修错）

| 旧说法 | 现码 |
|--------|------|
| 短线 K 线死盯 Hyperliquid | 已跟活跃所，可用 `SCALP_KLINE_EXCHANGE` |
| 短线同币对冲幽灵单 | 已禁反手、同向最多 1 仓 |
| 中线还是 SwingAgent 每 tick LLM 开仓 | 模块弃用；开仓走因子路由（开关开时） |
| 中线意图转化 0% | 8 月 17 日周报 67.3% |
| 中线 20x 小币杠杆 | 8 月 14 日改造稿已声明近 14 天全 10x；现码因子路由档位另算 |
| 中线并进 long、`_run_mid` 恒 False | 现码已接回 mid 调度 + 因子路由；旧测试过期 |

---

## 7. 建议改的顺序（只排序，方案见设计文档）

1. **止血（现码 bug，不依赖新数据）**  
   短线：S1 涨跌补数、S2 否决抹仓、S3 预算 mode、S8 总控对短线 close 真跳过。  
   中线：M1 因子仓禁止 LLM `trend_*` 碎平（先规则/因子失效再平）、M2 持仓互锁改成「同 nature」、M3 路由算因子与成交同所。
2. **对齐入场与仓位**  
   短线：分数未校准前仓位不跟高分放大（S7）。中线：止盈止损不要写死 5/10（M4）；震荡硬拦或只影子（M6）。
3. **证据**  
   重跑短线日检、中线周报（系统身份 + `paper_orders`），再决定要不要加因子/加门。

配套设计：[`短线中线修复升级设计_20260820.md`](./短线中线修复升级设计_20260820.md)。
