# 数据库备份与恢复手册

> 关联：整改项 R2（备份自动化）｜脚本：`scripts/db_backup.ps1`
> 4 库：`alpha_arena`（主）/ `alpha_market`（行情）/ `alpha_analytics`（分析）/ `alpha_snapshots`（快照）

## 1. 前置条件（一次性）

备份账号必须能读取全部表。应用库启用了**租户 RLS**（`account_asset_snapshots` 等表带行级安全策略），
普通应用账号（如 `laobao`，无 `BYPASSRLS`）执行 `pg_dump` 会报
`查询将被表 ... 的行级安全性策略所影响`。

**决策（2026-08-13，用户确认）：采用方案 A。** 需用 PostgreSQL 超级用户执行一次：

```sql
-- 方案 A（已选定）：给应用账号加 BYPASSRLS
--   注意：应用代码本身按 tenant_id 过滤（README 租户隔离），RLS 是纵深防御；
--   BYPASSRLS 只影响「该账号连库时的 RLS 豁免」，不改变应用内过滤逻辑。
ALTER ROLE laobao BYPASSRLS;
-- 验证：
-- SELECT rolname, rolbypassrls FROM pg_roles WHERE rolname='laobao';
```

（方案 B 专用备份角色见 git 历史/设计文档，本轮不采用。）

## 2. 每日备份（决策后的策略）

**策略（2026-08-13 用户确认）**：`alpha_market`（36GB）**每周日全量** + 其余三库**每日**。

```powershell
# 每日任务（三库：arena/analytics/snapshots；market 自动 SKIP）
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\db_backup.ps1

# 每周日（含 market 全量，耗时长，安排在低峰）
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\db_backup.ps1 -IncludeMarket
```

- 输出：日备 `postgres_backup\<yyyyMMdd>\<库名>.dump`；market 周备 `postgres_backup\market_weekly\<yyyyMMdd>\`
- 保留：日备 7 天、market 周备 21 天（3 周）；每周日额外 `postgres_backup\globals\globals_<日期>.sql`
- 日志：`logs/db_backup.log`
- 任务计划（**脚本跑通后**再注册）：

```powershell
schtasks /Create /TN "AlphaArena-DailyDBBackup" /SC DAILY /ST 02:00 `
  /TR "powershell -NoProfile -ExecutionPolicy Bypass -File D:\001Alpha\Hyper-Alpha-Arena\scripts\db_backup.ps1"
schtasks /Create /TN "AlphaArena-WeeklyMarketBackup" /SC WEEKLY /D SUN /ST 03:00 `
  /TR "powershell -NoProfile -ExecutionPolicy Bypass -File D:\001Alpha\Hyper-Alpha-Arena\scripts\db_backup.ps1 -IncludeMarket"
```

## 3. 恢复步骤（演练用）

```powershell
# 1) 建临时恢复库（用超级用户）
psql -h localhost -U postgres -c "CREATE DATABASE alpha_arena_restore_test;"

# 2) 恢复
set PGPASSWORD=<密码>
"C:\Program Files\PostgreSQL\15\bin\pg_restore.exe" -h localhost -U laobao -d alpha_arena_restore_test --no-owner "postgres_backup\<日期>\alpha_arena.dump"

# 3) 校验行数（主表）
psql -h localhost -U laobao -d alpha_arena_restore_test -c "SELECT count(*) FROM users; SELECT count(*) FROM positions;"

# 4) 清理
psql -h localhost -U postgres -c "DROP DATABASE alpha_arena_restore_test;"
```

## 4. 每月演练清单

- [ ] 最近一次备份目录存在且 4 个 dump 均 >1MB（`logs/db_backup.log` 无 FAIL 行）
- [ ] 按 §3 恢复 `alpha_arena` 到临时库成功，`users`/`positions` 行数 >0
- [ ] 恢复库立即删除（避免数据泄漏面）
- [ ] 任务计划存在：`schtasks /Query /TN AlphaArena-DailyDBBackup`

## 5. 当前状态（2026-08-13 深夜更新）

| 项 | 状态 |
|---|---|
| 脚本 | ✅ 已落地（market 周备模式 + RLS 失败检测 + 锁超时） |
| 角色决策 | ✅ 用户已选方案 A（`ALTER ROLE laobao BYPASSRLS`）——**待超级用户执行** |
| 首次真实备份 | ⏳ 执行 ALTER ROLE 后跑 `db_backup.ps1` 验证三库成功 |
| 任务计划 | ⏳ 三库验证通过后注册（每日 02:00 + 周日 03:00 market） |
| 恢复演练 | ⏳ 首次备份后执行 |
| 磁盘预算 | market 周备 ×3 周 + 日备 ×7 天，预计 50–100GB，纳入 R6 告警监控 |
