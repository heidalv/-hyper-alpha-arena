# 中线因子路由上线验证报告 2026-08-15

## 结论

中线因子系统已完成从「研究空转」到「闭环交易」的转变：因子路由（factor_route）上线，
首笔纸盘交易于 2026-08-15 12:04:39 成交（ETH swing buy @ 1882.4，10x，margin $32）。

## 本轮修复（6 项）

| # | 问题 | 修复 | 提交 |
|---|---|---|---|
| 1 | legacy 快照型因子无法回测（900 根只有 1 个有效值）| `_rolling_recompute` 滚动重算构造历史序列 | 75484a7 |
| 2 | rejected 引用记录不重开 → 每日扫描空转 | `register_reference` 自动重开 | 75484a7 |
| 3 | ai_generated 误入滚动路径触发 pandas 深递归 | 滚动仅限 legacy_compat | eb5c9ce |
| 4 | registry 活跃因子无复检/退役路径 | `recheck_and_prune` 补 registry 复评分支 | 0305d85 |
| 5 | factor_route 接线（决策→执行→风控全链路）| `midlong_factor_route.py` + mlto_cycle/midlong_loop/executor | afa17a8 |
| 6 | 每笔净利双重扣费门槛（拒掉 sharpe 2+ 因子）| `avg_net_per_trade > buffer`（成本只扣一次）| 183d709 |
| 7 | 冷池前视因子未拦截（liq_magnet_rev 用 shift(-2) 作弊，ICIR 2.47 全假）| 冷池源码级 shift(-N) 静态拦截 | 183d709 |
| 8 | 重启即重扫（weekly_evolution 首跑 60s 触发 1-2 小时扫描）| 落盘标记每日去重闸门 | 3d59884 |

## 因子池状态（2400 根样本外复检后）

| 因子 | 周期 | 级别 | IC | OOS sharpe | 状态 |
|---|---|---|---|---|---|
| macd@4h | 4h | **A** | -0.118 | 0.83 | ✅ 活跃 |
| atr@4h | 4h | C | -0.068 | -0.14 | 降级候选（900 根 B 为样本不足的乐观估计）|
| parkinson_vol@4h | 4h | C | 0.025 | -0.10 | 降级候选 |
| ai_gen_extreme_reversal@1d | 1d | C | 0.030 | -0.69 | 降级候选 |

复检降级是**正确行为**：2400 根（4h=400 天）样本下只有 macd@4h 的 OOS 绩效稳健。
alpha101 47 个动量型公式在此市场窗口（均值回归 regime）全灭——真实市场结论。

## factor_route 运行证据

- `mid_mode=factor_route`（前端「中线因子路由」），中线宇宙 = 固定 mid BTC/ETH/SOL。
- 每 2 分钟 tick：macd@4h 单因子投票（|IC| 加权、OOS 方向定向、尾部 z-score）。
- 决策示例：BTC buy(0.63) / ETH buy(0.37) / SOL hold(-0.10)。
- 执行链验证：authority ✓ → regime=ranging probe（margin 0.03）✓ → 组合敞口 30%<150% ✓
  → V5Gate RR 2.86≥2.5 ✓ → StrictData 指标注入 ✓ → **模拟下单成功**。
- 风控拦击（正常）：BTC 240 分钟同向冷却；net_exposure 150% 上限（修小保证金档前）。

## 遗留事项

1. **跨服务 DB idle-in-transaction 泄漏**（strategy_templates/decision_snapshots/
   signal_trade_feedback/full_auto_sessions/dingtalk_bots 的启动同步路径）——
   扫描路径本身无泄漏（kline 缓存 + with 会话），建议单独审计。
2. 扫描（84 条 × 2400 根）在交易时段与实盘循环抢资源，建议挪到凌晨。
3. 因子池需持续扩源：冷池（去前视后）与 registry 每日扫描继续产出候选；
   alpha101 库偏动量，可补反转型公式。
