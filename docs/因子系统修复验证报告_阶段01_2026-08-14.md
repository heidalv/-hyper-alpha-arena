# 中短线因子系统修复 · 阶段 0+1 验证报告（2026-08-14）

> 范围：阶段 0（基线仪表）+ 阶段 1（P0-1 DSR 闸门、P0-2 命名空间/类别/白名单统一、P1-A1 evaluator 前瞻期）。
> 依据：`docs/中短线因子系统修复设计与执行计划_2026-08-14.md`

## 一、交付 commit（分支 remediation/midlong-fixes）

| Commit | 内容 |
|---|---|
| `2243d06` | 回归测试脚手架 `backend/tests/unit/test_factor_pipeline_gates.py`（10 用例） |
| `fd83033` | P0-1 DSR/PBO 闸门修复 + P1-A1 evaluator 前瞻期修复（dsr_pbo / scorer / evaluator / settings） |
| `f48c962` | P0-2 键归一化 + 类别别名 + 白名单优先语义（key_utils / base_factors） |
| `441bd5f` | 阶段 0 管线健康自检 + V3 精选灰度入口（pipeline_health / main / ops_routes / v3_factor_pipeline） |
| `08d0249` | **P0-2 根因**：精选白名单误用 Market 库会话查 Analytics 表（scalp_factor_exclude） |

## 二、修复内容摘要

1. **P0-1**：`compute_pbo_simple` 返回显式 `indeterminate`；`_dsr_pbo_gate` 跨币样本 < `FACTOR_SCORER_DSR_MIN_SYMBOLS`(4) 时统一 fail-open 跳过 + WARNING（单币/3币一致化），充足时 `pbo<=max_pbo` 判定；跳过原因落库 `scores.reason`。
2. **P0-2**：
   - `key_utils.normalize_engine_key`：FACTORS 键（`evo_*`）与精选白名单（裸 id）归一化比对；
   - `_CATEGORY_ALIASES`：technical→TREND、composite→STRENGTH、discovered/alpha101→MOMENTUM，未知类别 WARNING；
   - `compute_all_factors` 白名单优先语义（命中白名单不受类别排除影响）；
   - **根因修复**：`get_scalp_factor_allowlist` 改用 `AnalyticsSessionLocal`（FactorActiveSet 在 Analytics 库），并补失败告警。
3. **P1-A1**：`get_factor_evaluator` 每次调用同步更新 forward_period；scorer 显式传 `forward_period=fwd`。
4. **阶段 0 仪表**：启动自检（白名单空命中/空白名单+DB 有因子 → ERROR 告警）；`/api/ops/factor-pool` 新增 `pipeline_health` 字段；`SCALP_VETTED_IN_V3` 灰度开关（默认 0）。

## 三、验证结果

### 单元测试
- 新增 `test_factor_pipeline_gates.py`：**10/10 通过**。
- 相关回归（factor_card / evo_gate_roots / wfo_ic / signal_comprehensive）：177 通过；**5 失败为预存在问题**——用 `git stash` 回退本次改动后在基线上复现完全相同的 5 失败（wfo_ic×4、factor_registration_count×1），与本批改动无关。
- `compileall`：除历史遗留 `_ai_gen_quarantine/`（含空格的类名语法错误，验证报告已记录排除）外零错误。

### 运行冒烟（后端已重启，新代码生效）
| 检查项 | 修复前 | 修复后 |
|---|---|---|
| `/api/ops/factor-pool.pipeline_health.allowlist_hits` | 0（且白名单恒空） | **3/3**（d6f82d364676127e / seed_rev20 / seed_rev50） |
| 启动自检日志 | 无 | `[FactorPipeline] 精选白名单命中 3/3（engine 因子 46）` |
| 短线扫描因子计算耗时（backend.log ScalpProfile） | `factors=0.00`（白名单空集全拦，score=0 恒 hold） | `factors=0.01`（3 个精选因子恢复计算） |
| 中长线候选验证理由 | 14 个全为机械性 `pbo=0.500` | 实测 3 因子：真实指标驱动的 C 级 + `DSR/PBO 跳过（跨币样本不足 fail-open）`（如 rev_z_4h：IC=0.169/ICIR=0.71 达标但 OOS_sharpe=-1.70 经济性拒绝） |
| 后端健康 | healthy v0.7.0 | healthy v0.7.0（重启后 15-20s 恢复） |

## 四、冒烟中确认的下一批问题（已记录，未在本阶段修复）

- **P2-1 实锤**：`register` 幂等更新保留旧 status → 重灌 alpha101 后 0 个候选可验证（`VALIDATE: scored=0`），"一键挖矿"二次空转。已列入计划阶段 6（或可提前）。
- 进化 source 字符串（rev50/seed_bootstrap…）触发新加的类别告警（预期行为，白名单优先语义下无实际影响）。
- 后端重启方式：本阶段因 `start-dev.ps1` 编码问题改用手动 kill+启动（与原脚本同环境变量）；看门狗（PID 16456）继续托管端口 8000，假死可自动拉起。

## 五、回滚

- 代码：`git revert 08d0249 f48c962 fd83033`（测试与仪表 commit 可选保留）。
- 行为开关：`FACTOR_SCORER_DSR_MIN_SYMBOLS`、`SCALP_VETTED_IN_V3=0`（未启用）可直接调回。

## 六、下一步

按计划进入**阶段 2（信号方向修复，P1-E1~E4）**：funding 单位统一、is_directional 过滤、regime 权重乘数归一、D7 引导复活，全部配 env 灰度 + paper 观察。
