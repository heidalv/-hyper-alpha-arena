# 加密货币交易专有模型训练方案

> 版本: v1.0
> 日期: 2026-04-15
> 目标: 使用 Unsloth + Qwen3.5-9B 在 2x2080Ti(44GB) 上训练

---

## 0. 数据资产

- 总记录: 34,647 条
- 覆盖: BTC, ETH, ASTER, VIRTUAL, SOL, BNB, XPL
- 字段: prompt_snapshot, reasoning_snapshot, decision_snapshot

---

## 1. 硬件适配

推荐: Qwen3.5-9B + Unsloth 4bit QLoRA
显存占用: ~18GB (44GB 绰绰有余)

---

## 2. 数据提取脚本

保存到: D:\\LLM\\extract_training_data.py

```python
import sqlite3
import json
import os

DB_PATH = r"d:\001Alpha\Hyper-Alpha-Arena\data\alpha_arena.db"
OUTPUT_DIR = r"D:\LLM\training_data"

def extract_decisions():
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()
    cursor.execute("""
        SELECT id, decision_time, symbol, operation,
               prompt_snapshot, reasoning_snapshot, decision_snapshot
        FROM ai_decision_logs
        WHERE prompt_snapshot IS NOT NULL
          AND reasoning_snapshot IS NOT NULL
          AND decision_snapshot IS NOT NULL
    """)
    rows = cursor.fetchall()
    conn.close()
    return rows

def format_conversation(row):
    id_, dt, symbol, operation, prompt, reasoning, decision = row
    return {
        "conversations": [
            {"from": "system", "value": "你是一个专业的加密货币交易决策助手。"},
            {"from": "human", "value": prompt},
            {"from": "gpt", "value": f"【分析推理】{reasoning}\n\n【交易决策】{decision}"}
        ],
        "id": id_, "symbol": symbol, "operation": operation
    }

def main():
    os.makedirs(OUTPUT_DIR, exist_ok=True)
    rows = extract_decisions()
    print(f"提取到 {len(rows)} 条记录")
    
    formatted = [format_conversation(r) for r in rows if format_conversation(r)]
    import random
    random.shuffle(formatted)
    n = len(formatted)
    
    for data, fname in [
        (formatted[:int(n*0.8)], "train.jsonl"),
        (formatted[int(n*0.8):int(n*0.9)], "val.jsonl"),
        (formatted[int(n*0.9):], "test.jsonl")
    ]:
        with open(os.path.join(OUTPUT_DIR, fname), 'w', encoding='utf-8') as f:
            for item in data:
                f.write(json.dumps(item, ensure_ascii=False) + '\n')
        print(f"保存 {len(data)} 条到 {fname}")

if __name__ == "__main__":
    main()
```

---

## 3. 微调脚本

保存到: D:\\LLM\\finetune_qwen35_trading.py

```python
from unsloth import FastLanguageModel
from unsloth import is_bf16_supported
from datasets import load_dataset
from trl import SFTTrainer
from transformers import TrainingArguments
import torch

MODEL_NAME = "qwen3.5-9b"
OUTPUT_DIR = "./qwen35-trading-v1"
MAX_SEQ_LENGTH = 8192

print("1. 加载模型...")
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name=MODEL_NAME, max_seq_length=MAX_SEQ_LENGTH,
    dtype=None, load_in_4bit=True,
)

print("2. 添加 LoRA...")
model = FastLanguageModel.get_peft_model(model, r=32, target_modules=["q_proj", "k_proj", "v_proj", "o_proj"])

print("3. 加载数据...")
train_ds = load_dataset("json", data_files="./training_data/train.jsonl", split="train")
val_ds = load_dataset("json", data_files="./training_data/val.jsonl", split="train")

def format_prompt(example):
    prompt = ""
    for conv in example["conversations"]:
        role_map = {"system": "system", "human": "user", "gpt": "assistant"}
        prompt += f"<|im_start|>{role_map[conv['from']]}\n{conv['value']}<|im_end|>\n"
    return {"text": prompt}

train_ds = train_ds.map(format_prompt)
val_ds = val_ds.map(format_prompt)

print("4. 开始训练...")
trainer = SFTTrainer(
    model=model, tokenizer=tokenizer, train_dataset=train_ds, eval_dataset=val_ds,
    dataset_text_field="text", max_seq_length=MAX_SEQ_LENGTH, packing=True,
    args=TrainingArguments(
        output_dir=OUTPUT_DIR, num_train_epochs=3, per_device_train_batch_size=2,
        gradient_accumulation_steps=4, learning_rate=2e-4,
        fp16=not is_bf16_supported(), bf16=is_bf16_supported(),
        logging_steps=10, save_steps=100, eval_steps=100,
    ),
)
trainer.train()

print("5. 保存...")
trainer.save_model(f"{OUTPUT_DIR}/lora_model")
tokenizer.save_pretrained(f"{OUTPUT_DIR}/lora_model")

print("6. 合并导出...")
merged_model, merged_tokenizer = FastLanguageModel.merge_weights(model, tokenizer)
merged_model.save_pretrained_merged(f"{OUTPUT_DIR}/merged_model", merged_tokenizer, save_method="merged_4bit")
print(f"完成! 模型保存在: {OUTPUT_DIR}/merged_model")
```

---

## 4. Ollama 导出

```bash
# 创建 Modelfile
cd D:\LLM\qwen35-trading-v1

# Modelfile 内容:
FROM ./merged_model
PARAMETER temperature 0.7

# 创建模型
ollama create qwen35-trading-v1 -f Modelfile

# 测试
ollama run qwen35-trading-v1 "BTC当前趋势如何？"
```

---

## 5. 实施路线图

```
Week 1: python extract_training_data.py
Week 2: python finetune_qwen35_trading.py
Week 3: 质量优化
Week 4: Ollama 导出 + Alpha Arena 集成
```

---

## 6. 快速开始

```
1. python extract_training_data.py
2. python finetune_qwen35_trading.py
3. ollama create qwen35-trading-v1
4. Alpha Arena 配置 API
```