# 中线弹药生产修复 · 验证报告（2026-08-14 下午）

> 背景：中线因子化已拍板、旧 AI 中线已停，但弹药管道空转（验证任务 scored=0）。
> 本批修复：P2-1 重开机制 + registry Python 类因子接入闸门管道。

## 一、交付 commit（分支 remediation/midlong-fixes）

| Commit | 内容 |
|---|---|
| `1c6d841` | P2-1：register 公式变化重开 + reopen_rejected + seed_alpha101(reopen) |
| `54a7000` | 弹药扩源：midlong_registry_factors 管道 + 引用记录 + 一键挖矿接线 + 5 个测试 |

## 二、修复内容

1. **P2-1（空转根因）**：`register` 在公式实际变化时重置 status/grade/scores 为 candidate（公式未变保持幂等）；新增 `reopen_rejected(tenant_id, category)` 批量重开被拒因子；`seed_alpha101(reopen=True)`；一键挖矿端点带 reopen。
2. **弹药扩源**：42 个 registry Python 类因子（22 ai_generated + 20 legacy_compat，按模块路径精确识别）以引用记录（kind=registry、无公式、`@tf` 分周期、`registry_factor_id`）登记进目录；`scan_registry_midlong` 复用同一闸门引擎（FactorEvaluator IC/ICIR + `_walk_forward_backtest` OOS + 相同分级门槛）打分回写；A/B 晋升 active。
3. **生效链路**：`build_snapshot` 按 `registry_factor_id` 经 `factor_service.compute` 计算；`_load_active_custom_factors`/`recheck_and_prune` 对无公式引用记录正确跳过。
4. **任务化**：`factor_jobs.run_scan_registry_midlong`；一键挖矿 = reopen + seed(alpha101+registry) + 双队列（validate + scan）。

## 三、真实数据结果（关键）

| 项 | 结果 |
|---|---|
| alpha101 重评 | 23 个全部重评（管道复活，此前 scored=0）→ **0 晋升**（真实拒绝：IC 弱/负、OOS sharpe 负） |
| registry 首扫 | 登记 84 条（42 因子 × 4h/1d），评分 79 → **1 晋升** |
| **第一颗弹药** | **`ai_gen_extreme_reversal@1d`**：grade B，IC=0.0366，OOS sharpe=0.6735 |
| 因子池现状（ops 面板实锤） | **active=1** / candidate=0 / rejected=106；by_timeframe 1d=1 |
| 生效验证 | 后端重启后 `/api/ops/midlong-factors` top_active 含该因子，runtime_weight=1.0；经 `build_snapshot` 注入中长线决策 |

## 四、测试

- `test_midlong_ammo.py`：5/5（公式变化重开 / 同公式保持 / reopen 批量 / 引用记录 / 分级门槛）。
- compileall OK。

## 五、说明与下一步

- **弹药质量**：目前只有 1 颗（1d 周期、IC 中等、sharpe 0.67）。要凑到设计要求的 active≥5，需要：① 扩充 alpha101 变体参数域（窗口/组合）；② 4h 周期的趋势类因子（当前 4h=0）；③ 观察 evo 进化回路今晚 03:00/04:00 的产出。
- **registry 扫描是离线重活**（84 条 × 3 币 ≈ 数分钟），已任务化，建议每日调度跑一次（可挂 evolution_scheduler）。
- 冒烟脚本保留：`scripts/_smoke_ammo_scan.py`。
