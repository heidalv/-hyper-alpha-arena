# 中短线因子系统修复 · 阶段 5 验证报告（2026-08-14）

> 范围：阶段 5 = 块 F/G（P1-G1 DSL 前视 + P1-H1~H4 调度/并发/鉴权）。
> 依据：`docs/中短线因子系统修复设计与执行计划_2026-08-14.md`

## 一、交付 commit（分支 remediation/midlong-fixes）

| Commit | 内容 |
|---|---|
| `7924dbc` | P1-G1 cs_rank 默认抛错 + audit 拦截 + 三个矿机算子池剔除 + 5 个测试 |
| `bd52919` | P1-H1 TOCTOU + P1-H2 紧急冷却独立时间戳 + P1-H3 端点鉴权 + P1-H4 single-flight |
| `1117011` | audit 禁用检查前移（修复 cs_rank 漏拦） |

## 二、修复内容

1. **P1-G1**：
   - `expr/ops.cs_rank`：默认抛错（全序列分位排名=未来函数）；`FACTOR_EXPR_RANK_SINGLE_SERIES=1` 时退化为滚动 `ts_rank`（因果）。
   - `expr/audit`：rank/cs_rank/scale 在单序列上下文直接拒绝（负参数检查对它们零覆盖；修正了 cs_rank 因不在 WINDOW_OPS 集合而漏拦的问题）。
   - 三个矿机（gp/alpha/mcts）算子池剔除 `LOOKAHEAD_BANNED_OPS`，避免随机生成必被审计拒绝的个体。
   - 现库核查：仅 3 个 QUARANTINE 因子使用 rank/cs_rank（实盘 3 个 PAPER 因子干净）→ audit 拦截使其无法再被进化回路重晋升（含前视因子的正确 fail-closed）。
2. **P1-H1**：`weekly_evolution` 与 `trigger_emergency_evolution` 的检查+置位同处锁临界区（原锁外检查可双线程并发跑完整进化）；起线程失败释放预留位。
3. **P1-H2**：紧急进化 24h 冷却改读 `evolution_events(evolution_type='emergency')` 最新记录，不再读被 weekly 污染写回的 `SystemCoordinatorState.last_evolution_at`。
4. **P1-H3**：`/validate/{id}`、`/validate`、`/alpha101/seed`、`/alpha101/validate` 四个端点补 `require_user_tenant`（原无鉴权且固定操作管理员租户）。
5. **P1-H4**：调度侧因子发现闸门改走 `factor_job_manager` single-flight 队列（与 API 队列互斥，重复触发复用进行中任务）。

## 三、验证结果

### 单元测试
- 新增 `test_factor_phase5.py`：**5/5 通过**（cs_rank 默认抛错 / env 退化因果 / audit 拦三算子 / 滚动算子放行 / parse 抛 ExprError）。
- 回归：factor_card 21/21、mcts_miner 全过、phase-5 相关全过。
- **环境限制说明**：`test_gp_miner.py::test_gp_miner_eval_population_loky_parallel` 在本沙箱失败于 loky 多进程 named-pipe（WinError 5，沙箱禁命名管道），非代码问题——修复前该测试还多一层"生成 rank 个体被 audit 拒"的真实失败，现已消除（生成种群全部为合法算子）。

### CLI 冒烟
| 检查项 | 结果 |
|---|---|
| audit(rank/cs_rank/scale) | 三者全部 `ok=False` + 明确前视禁用理由 |
| parse(rank 表达式) | ExprError（fail-closed 拦截成功） |
| audit(ts_rank/mean 滚动算子) | 放行 |

### 运行冒烟（后端已重启）
| 检查项 | 结果 |
|---|---|
| POST /api/factors/alpha101/seed（未认证） | **401 拦截** |
| POST /api/factors/validate/{id}（未认证） | 拦截 |
| /api/ops/factor-pool pipeline_health | allowlist 3/3 命中保持 |

## 四、说明与遗留

- P1-H1/H2/H4 属调度线程路径（夜间 03:00/04:00 与紧急触发），代码级修复 + 编译/静态验证完成；行为验证依赖后续调度运行观察。
- 紧急进化冷却此前若已被 weekly 误设（`last_evolution_at` 刚更新），改用独立时间戳后首次调用立即生效，无需迁移。
- rank 前视的存量影响：3 个 QUARANTINE 因子不再可被重晋升（预期）；`real_factor_backtest.py` 研究脚本中的 rank 用法现在会抛错（研究侧需改用 ts_rank 或显式开 env 开关）。
- 冒烟脚本保留：`scripts/_smoke_phase5_dsl.py`。

## 五、下一步

阶段 6（加固批，P2/P3 批量 + 阈值/状态 SSOT + 因子表迁移 + JSON 目录加固）：按计划分批进行，预计 1-2 周；可先做高价值项：P2-1（register 重置状态，解除"一键挖矿"空转）、阈值 SSOT、MACD 修复、factor_cache 序列化、JSON 目录绝对路径/备份。
