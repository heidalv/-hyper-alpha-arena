# Phase F 实施记录（2026-08-16）—— long_trend_v2 实盘接线（env 开关，默认关）

## 产出
- 新增 `backend/services/long_trend_v2.py`：V2 集成层
  - entry_gate：tier=long 要求 trend_layer L1=up（多头单边，禁做空）。
  - manage_long_position：每日管理（Chandelier 止损 / 结构破坏唯一退出 / 新高金字塔）。
  - 数据源：实盘交易所 1d（决策判当前市况，非 binance 回测源）。

## 接线点（三处，全部 LONG_TREND_V2=1 才生效）
1. `midlong_executor.execute_midlong_open`：tier=long 时加 L1=up 入场闸。
2. `full_auto_trading_service._run_midlong_active_exit`：long 仓改走 V2 管理器
   （跳过 bias 反转 / no_progress）。
3. `midlong_position_manager.manage_position`：long 仓跳过短中线口径
   （分档 TP / 保本 / 15min 复查）。

## 验证
- 编译 0 失败；回归测试 27/27 通过。
- 入场闸实测（当前市况）：BTC/SOL/ASTER L1=down、ETH sideways → buy 全部拦截、
  sell 全部拦截（多头单边正确）——当前市场非 up，V2 正确地不开长线多仓。
- 后端已重启（PID 9260，V2 默认关，旧路径行为不变）。

## 启用状态（用户确认：启用，不用旧链路）
- .env 已加 `LONG_TREND_V2=1` 并重启（PID 17184，health 200）。
- 旧长线路径已全部旁路：入场闸 + active_exit 路由 + mode-B 跳过 + hold-timeout 跳过。
- 当前市况 L1=down/sideways（BTC/SOL/ASTER down、ETH sideways）→ V2 正确地
  **不开长线多仓**，等趋势转 up 才出手；现有 10 仓均为 scalp/short + swing/mid。
