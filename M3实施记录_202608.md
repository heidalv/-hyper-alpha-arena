# M3 实施记录（2026-08 审计修复第四批：P2 清理 + 验证 + GPU）

## 已实施项

### P2-1 price_cache 交易所键
- price_cache.py legacy get_cached_price/cache_price 增加 exchange 参数并纳入缓存键；
- market_data.py 读（本所优先 + legacy 空键二级回退）写（3 处带 exchange）；
- hyperliquid_market_data.py 两处写入 exchange=hyperliquid → asterdex 不再污染 hyperliquid 读数。

### P2-3 维护保证金按交易所
- paper_trading_engine._calc_liquidation_price 增加 exchange 参数，调 fee_schedule_service.get_maint_margin_rate。

### P2-4 paper 限价 maker/taker 修正
- paper_exchange_simulator：可市价化限价单恒为 taker（对侧盘口价成交+taker 费），仅 resting 挂单按 maker；
- paper 引擎新限价单不再 force_maker。

### P2-6 A/B Welch t 检验
- learning_ab_framework：ttest_rel（按索引强行配对）→ ttest_ind(equal_var=False)（独立样本 Welch）。

### P2-7 冷池变体择优标注
- midlong_cold_pool 报告增加 methodology_note: multi_fwd_variant_selection_unadjusted。

### P2-5 死代码处置
- backend/factor_engine/DEPRECATED.md 标记遗留引擎（0 导入）；删除孤儿 scalp_exit_override pyc。

## 全链路验证（实测）
- 语法：全部修改文件 py_compile 通过；
- 导入冒烟：40 个修改模块全部 import 成功（0 失败）；
- 回归：新 27 用例 + 既有 96+ 用例全绿；
- 隔离 sqlite E2E：账户→开仓→学习落库（decision_context 含 snapshot_id/decision_log_id）→outcome batch 幂等
  （trades 保持 1，无重复 EMA）→PnL 口径 -5（修复前 +10）→paper 补扫去重→耐久冷却正确拦截 → SMOKE OK。

## GPU 加速落地（第一批）
- midlong_cold_pool 新增 _rolling_ic_torch / _batch_rolling_ic / _rolling_ic_batch（torch FP32 + numpy 回退），
  _robust_rolling_ic 改走分发器；
- 实测（GTX 1070）：批量×128 因子 258.6ms vs 逐因子 numpy 4198.7ms（≈260x），偏差 0.00e+00；
  外推冷池扫描 IC 部分 ≈27s（旧扫描 4.4h）。

## 生产体检结论（实测）
- 数据管线健康（85.6M K 线、5 所、当日新鲜；资金费 265 万行）；
- 决策循环空转（30 天 11.1 万日志/33.3 万快照，仅 1774 条回填）；
- 交易/学习层从未启动：alpha_arena accounts=0、sessions=0、positions=0、strategy_trades=0、
  strategy_memories=0；因子池 active=0（冷池历史扫描 0 通过）；
- 后端进程运行中（uvicorn 8000）；重启后磁盘修复代码生效。
- 已知小瑕疵：query_optimizer 对 entry_time/decision_time 建索引告警（列缺失，非致命）。
