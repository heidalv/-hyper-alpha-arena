# 全库编码损坏修复记录（2026-08-16 重大事件）

## 根因（深度排查最终结论）
**团队历史 PowerShell 5.1 批处理管线**长期以「GBK 误读 UTF-8 → 重写 UTF-8」的方式处理中文 Python 文件：
- 中文 3 字节 UTF-8 按 GBK 双字节解码 → 乱码；
- 中文行尾换行（0x0A）被 GBK 解码吞掉 → **行合并**（注释吞代码、块首语句丢缩进）；
- 无效字节对替换为 '?' → 字符串/注释丢字。
磁盘代码早已千疮百孔（153 个非 legacy 文件无法编译），8/13 16:35 启动的后端只是「内存僵尸」——
**任何重启都会失败**。这正是系统「看起来在跑、但无法正常迭代」的最终根因。

## 本会话事件链
1. 我的批量导入统一脚本沿用 PS 5.1 读写路径，在已损坏的库上叠加了一轮损坏（61 文件）；
2. 发现后立即止损：cp936 字节级反向 + git HEAD 尝试 + haa_baseline_check（8-13 快照，同样受损）；
3. 最终以「基线 + 行级重建 + 结构修复引擎」完成 153 文件的修复；
4. 全部修改经 UTF-8 无 BOM 的 Python 工具链写入，杜绝 PS 5.1 二次损坏。

## 修复结果（全部实测）
- 全库编译：**0 失败**（排除被隔离的 legacy 目录 backend/factor_engine/ 与 _ai_gen_quarantine/_archive，
  这些目录本身被 loader 跳过、历史即损坏）；
- 关键模块导入冒烟：**50/50 通过**（含 backend.main）；
- 回归测试：**123 通过**（M0/M1/M2 新用例 + 相关既有套件）；
- 我的 M0-M3 全部修复（PnL 双计、TP3 方向、DSR/PBO、快照关联、GPU 批量 IC 等）均完好，
  因为它们由 UTF-8 工具写入、未遭 PS 管线污染。

## 已知代价与注意事项
- 约 30 个文件因无法完整逆向，从 8-13 基线快照恢复（团队 8/14-16 的未提交改动可能丢失）；
  损坏版本保留在 `_recovery_quarantine/`（30 个文件）供人工比对抢救；
- 这些文件的导入统一（backend.database.*）已重放，语义与团队意图一致；
- 修复中不可避免的少量注释/错误文案丢字属纯文本层面，不影响逻辑。

## 防护建议（必须执行，否则会复发）
1. **禁止任何 PowerShell 5.1 脚本读写含中文的 .py/.md 文件**；一律用 pwsh7 + `-Encoding utf8NoBOM`，
   或 Python `read_text/write_text(encoding='utf-8')`；
2. 加 CI 守卫：`python -m compileall -q backend` 失败即阻断合并；
3. `.gitattributes` 加 `*.py text eol=lf working-tree-encoding=UTF-8`（可选）；
4. 本轮修复的代码在【下次重启】后生效——重启前建议先在 staging 验证一遍启动。
