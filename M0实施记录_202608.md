# M0 实施记录（2026-08 审计修复第一批：资金安全与风控）

> 依据：`交易审计修复设计文档_V1.md` M0 里程碑。全部修改只涉及记账与风控，不动学习闭环。

## 已实施项

### P0-6 PnL 双计修复（3 处）
- `backend/services/learning_loop_service.py` `_paper_position_pnl`：closed 仓位直接取 unrealized_pnl
  （close 落库口径已含 partial），禁止再叠加 partial_realized_pnl；仅列缺失时按价格差兜底。
- `backend/services/reentry_cooldown.py` `_durable_reopen_blocked`：pnl 只取 unrealized_pnl（删除 +partial 行）。
- `backend/services/paper_trading_engine.py` close_position 落库处：写入【P0-6 权威口径】注释。

### P0-7 出场执行修复（5 处）
- TP3 空单追踪止损漏乘 side_dir：`paper_trading_engine.py` 2459（staged_tp3）与 2519（unified_trailing）
  两处均改为 `_peak_price - _atr_price*trail_mult*_side_dir`。
- 锁利 SL 单调：4 处直接赋值（partial_close / profit_stage_close / breakeven_sl / tighten_sl）
  全部改走 `_tighten_sl_unified`（long 只升、short 只降）。
- 紧急 SL 杠杆感知：`unified_exit_executor._set_emergency_sl` 距离按 leverage 推导
  （min(5%, max(0.5/lev, 1%))），并在已知爆仓价时强制钳到爆仓价内侧（±0.5%×entry 缓冲）。
- 结构性加固：`update_position_tp_sl` 内部新增 `_ensure_sl_inside_liq` 守卫（覆盖 AI 主动调 SL 全路径）。
- 分段止盈跳档补齐：TP3/TP2 触发时按未触发的档位累计减仓比例一次执行
  （TP3 跳档补至 80%、TP2 跳档补至 50%），不再永久丢失 TP1/TP2 的 25%+25%。

### P0-8 资金费默认结算
- `backend/config/settings.py` 新增 `FUNDING_SETTLE_ENABLED`（默认 true）与 `FUNDING_SETTLE_APPLY_PNL`（默认 false）。
- `_maybe_settle_funding` 门禁从 `PAPER_SIMULATION_TIER=="research"` 改为 `FUNDING_SETTLE_ENABLED`；
  净值写入受 `FUNDING_SETTLE_APPLY_PNL` 控制（dry-run 观察期只写 ledger 不动余额）。
- `PaperFundingLedger` 模型 + 迁移 `add_funding_ledger_tier.py` 新增可空 `tier` 列（已注册进 migration_manager，
  幂等验证通过，当前库已生效）。

### P0-10 双重减仓收口
- `RISK_USE_LONG_TIER_STAGED_TP` 默认 true→false（settings.py:1787）。
- `midlong_position_manager._dim_staged_tp` 增加 v2 门：`RISK_V2_UNIFIED_STAGED_TP` 开启时直接 hold（双保险）。

## 回归测试
- 新增 `backend/tests/test_p0_fixes_regression.py`（10 个纯单元用例，全部通过）：
  PnL 双计语义×3、SL 单调×2、TP3 空单方向公式、紧急 SL 杠杆感知×2、开关默认值×2。
- 既有相关测试无回归：test_paper_netting / test_midlong_s2_exit_lifecycle /
  test_position_hold_time / test_fee_schedule_service 共 69 个全部通过。
- py_compile 全部修改文件通过。

## 上线说明（运维）
1. 资金费 dry-run：默认只记账（ledger.tier 可审计），观察两周纸面 vs 实盘资金费对拍后
   将 `FUNDING_SETTLE_APPLY_PNL=true` 计入净值。
2. 若要临时恢复旧行为：`FUNDING_SETTLE_ENABLED=false`（资金费）、
   `RISK_USE_LONG_TIER_STAGED_TP=true`（旧分段止盈，但 v2 门仍会短路——需同时关
   `RISK_V2_UNIFIED_STAGED_TP` 才真正回到旧双引擎行为）。
3. 紧急 SL 距离公式变化只影响「无 SL 且被硬闸门转设紧急 SL」的仓位，
   低杠杆/无 liq 信息的场景行为不变（仍是 5%）。
